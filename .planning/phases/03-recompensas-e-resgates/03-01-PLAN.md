---
phase: 03-recompensas-e-resgates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V3__create_reward_schema.sql
  - backend/src/main/java/br/com/habitinhos/rewards/Reward.java
  - backend/src/main/java/br/com/habitinhos/rewards/RewardRedemption.java
  - backend/src/main/java/br/com/habitinhos/rewards/RewardRedemptionStatus.java
  - backend/src/main/java/br/com/habitinhos/rewards/RewardRepository.java
  - backend/src/main/java/br/com/habitinhos/rewards/RewardRedemptionRepository.java
  - backend/src/main/java/br/com/habitinhos/rewards/RewardService.java
  - backend/src/main/java/br/com/habitinhos/rewards/RewardController.java
  - backend/src/main/java/br/com/habitinhos/rewards/dto/CreateRewardRequest.java
  - backend/src/main/java/br/com/habitinhos/rewards/dto/UpdateRewardRequest.java
  - backend/src/main/java/br/com/habitinhos/rewards/dto/RewardResponse.java
  - backend/src/test/java/br/com/habitinhos/rewards/RewardIntegrationTest.java
  - backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java
autonomous: true
requirements:
  - REWD-01
  - REWD-02
  - REWD-03
  - DOCS-01
user_setup: []
must_haves:
  truths:
    - "D-R01 Reward é template family-scoped criado pelo responsável autenticado."
    - "D-R02 Custo da recompensa deve ser inteiro positivo, com CHECK > 0 no banco."
    - "D-R03 Rewards são soft-deactivated (active=false), nunca deletados (ADR-008)."
    - "D-R08 coin_transactions.reward_redemption_id já existe em V2 como UUID nullable — V3 adiciona FK após criar reward_redemptions."
  artifacts:
    - path: "backend/src/main/resources/db/migration/V3__create_reward_schema.sql"
      provides: "Tabelas rewards e reward_redemptions com checks, FKs, índices e FK retroativa em coin_transactions"
    - path: "backend/src/main/java/br/com/habitinhos/rewards/Reward.java"
      provides: "Entidade JPA de template de recompensa"
    - path: "backend/src/main/java/br/com/habitinhos/rewards/RewardService.java"
      provides: "CRUD de recompensas para responsável + listagem para criança"
    - path: "backend/src/main/java/br/com/habitinhos/rewards/RewardController.java"
      provides: "Endpoints REST para recompensas"
  key_links:
    - from: "V3__create_reward_schema.sql"
      to: "RedemptionService in 03-03"
      via: "reward_redemptions schema contract"
      pattern: "reward_redemptions"
    - from: "RewardRepository"
      to: "RedemptionService in 03-03"
      via: "findByIdAndFamilyUnitIdAndActiveTrue"
      pattern: "findByIdAndFamilyUnitId"
---

<objective>
Criar o schema de recompensas e resgates, entidades, repositórios, CRUD do responsável e listagem da criança.

Purpose: dar aos planos 03-02 e 03-03 um modelo persistente e API de recompensas para REWD-01..REWD-03 e DOCS-01.
Output: migration V3, entidades, enums, repositórios, serviço, controlador, DTOs e testes de integração.
</objective>

<execution_context>
@/home/dmagliano/.codex/get-shit-done/workflows/execute-plan.md
@/home/dmagliano/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DECISIONS.md
@.planning/phases/03-recompensas-e-resgates/03-CONTEXT.md
@.planning/phases/03-recompensas-e-resgates/03-RESEARCH.md
@.planning/phases/03-recompensas-e-resgates/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-01-01: Criar migration V3 com contrato de recompensas e resgates</name>
  <files>backend/src/main/resources/db/migration/V3__create_reward_schema.sql, backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java</files>
  <read_first>
    @backend/src/main/resources/db/migration/V1__create_foundation_schema.sql
    @backend/src/main/resources/db/migration/V2__create_mission_coin_schema.sql
    @backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java
    @.planning/phases/03-recompensas-e-resgates/03-CONTEXT.md
  </read_first>
  <behavior>
    - D-R01/REWD-01: `rewards` armazena templates family-scoped com `title`, `description`, `cost`, `active`, `created_by_user_id` e timestamps.
    - D-R02/REWD-02: `cost` tem CHECK > 0.
    - D-R03/ADR-008: `active` default TRUE para soft deactivation.
    - D-R04/ADR-009/REWD-06: `reward_redemptions` tem `status` com CHECK IN ('REDEEMED', 'CANCELLED') — MVP sempre REDEEMED.
    - D-R08: V3 adiciona FK de `coin_transactions.reward_redemption_id` para `reward_redemptions(id)`.
    - Snapshot de título e custo no momento do resgate.
  </behavior>
  <action>Criar `V3__create_reward_schema.sql` sem editar V1 ou V2. `rewards`: `id UUID PRIMARY KEY`, `family_unit_id UUID NOT NULL REFERENCES family_units(id)`, `title VARCHAR(160) NOT NULL`, `description VARCHAR(1000)`, `cost INTEGER NOT NULL CHECK (cost > 0)`, `active BOOLEAN NOT NULL DEFAULT TRUE`, `created_by_user_id UUID NOT NULL REFERENCES app_users(id)`, `created_at TIMESTAMPTZ NOT NULL`, `updated_at TIMESTAMPTZ NOT NULL`; índices por `family_unit_id`, `(family_unit_id, active)`, `created_by_user_id`. `reward_redemptions`: `id UUID PRIMARY KEY`, `family_unit_id UUID NOT NULL REFERENCES family_units(id)`, `reward_id UUID NOT NULL REFERENCES rewards(id)`, `child_id UUID NOT NULL REFERENCES child_profiles(id)`, `wallet_id UUID NOT NULL REFERENCES wallets(id)`, `status VARCHAR(32) NOT NULL DEFAULT 'REDEEMED' CHECK (status IN ('REDEEMED', 'CANCELLED'))`, `snapshot_title VARCHAR(160) NOT NULL`, `snapshot_cost INTEGER NOT NULL CHECK (snapshot_cost > 0)`, `coin_transaction_id UUID REFERENCES coin_transactions(id)`, `created_at TIMESTAMPTZ NOT NULL`; índices por `family_unit_id`, `child_id`, `reward_id`, `coin_transaction_id`. Adicionar FK retroativa: `ALTER TABLE coin_transactions ADD CONSTRAINT fk_coin_transactions_reward_redemption FOREIGN KEY (reward_redemption_id) REFERENCES reward_redemptions(id)`. Atualizar `AbstractIntegrationTest.cleanDatabase()` para truncar `reward_redemptions`, `rewards` (após `coin_transactions`, antes de `wallets`).</action>
  <verify>
    <automated>cd backend && ./mvnw -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>Migration define tabelas rewards e reward_redemptions com checks, FKs, índices e FK retroativa em coin_transactions; cleanup inclui tabelas Phase 3.</acceptance_criteria>
  <done>Contrato de banco para recompensas e resgates existe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-02: Implementar entidades, enums e repositórios de recompensa</name>
  <files>backend/src/main/java/br/com/habitinhos/rewards/Reward.java, backend/src/main/java/br/com/habitinhos/rewards/RewardRedemption.java, backend/src/main/java/br/com/habitinhos/rewards/RewardRedemptionStatus.java, backend/src/main/java/br/com/habitinhos/rewards/RewardRepository.java, backend/src/main/java/br/com/habitinhos/rewards/RewardRedemptionRepository.java</files>
  <read_first>
    @backend/src/main/java/br/com/habitinhos/shared/model/BaseEntity.java
    @backend/src/main/java/br/com/habitinhos/missions/Mission.java
    @backend/src/main/java/br/com/habitinhos/missions/MissionRepository.java
    @backend/src/main/java/br/com/habitinhos/missions/AssignedMission.java
  </read_first>
  <behavior>
    - Reward segue padrão de Mission: BaseEntity, construtor protegido JPA, getters, domain methods update/deactivate.
    - RewardRedemption segue padrão de AssignedMission: snapshot fields, construtor que captura snapshot do Reward.
    - RewardRedemptionStatus enum: REDEEMED, CANCELLED.
    - RewardRepository segue padrão de MissionRepository: family-scoped queries.
    - RewardRedemptionRepository: family-scoped, por criança, por recompensa.
  </behavior>
  <action>Criar package `br.com.habitinhos.rewards`. `RewardRedemptionStatus`: `REDEEMED`, `CANCELLED`. `Reward` extends BaseEntity com `familyUnitId`, `title` (160), `description` (1000 nullable), `cost` (int), `active` (default true), `createdByUserId`; domain methods `update(title, description, cost)` e `deactivate()`. `RewardRedemption` extends BaseEntity com `familyUnitId`, `rewardId`, `childId`, `walletId`, `status` (default REDEEMED), `snapshotTitle`, `snapshotCost`, `coinTransactionId`; construtor recebe `Reward` e captura snapshot. `RewardRepository`: `findAllByFamilyUnitIdAndActiveTrueOrderByCreatedAtAsc`, `findByIdAndFamilyUnitId`, `findByIdAndFamilyUnitIdAndActiveTrue`. `RewardRedemptionRepository`: `findByIdAndFamilyUnitId`, `findAllByFamilyUnitIdAndChildIdOrderByCreatedAtDesc`.</action>
  <verify>
    <automated>cd backend && ./mvnw -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>Entidades compilam contra schema V3, enums correspondem aos checks SQL, repositórios dão os contracts consumidos por 03-02 e 03-03.</acceptance_criteria>
  <done>Modelo persistente de recompensa e resgate existe sem API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-03: Implementar CRUD de recompensas, listagem e testes de integração</name>
  <files>backend/src/main/java/br/com/habitinhos/rewards/RewardService.java, backend/src/main/java/br/com/habitinhos/rewards/RewardController.java, backend/src/main/java/br/com/habitinhos/rewards/dto/CreateRewardRequest.java, backend/src/main/java/br/com/habitinhos/rewards/dto/UpdateRewardRequest.java, backend/src/main/java/br/com/habitinhos/rewards/dto/RewardResponse.java, backend/src/test/java/br/com/habitinhos/rewards/RewardIntegrationTest.java</files>
  <read_first>
    @backend/src/main/java/br/com/habitinhos/children/ChildService.java
    @backend/src/main/java/br/com/habitinhos/children/ChildController.java
    @backend/src/test/java/br/com/habitinhos/children/ChildIntegrationTest.java
  </read_first>
  <behavior>
    - REWD-01: Responsável pode criar, editar, listar, ver e desativar recompensas.
    - REWD-02: Validação de custo positivo no DTO e no serviço.
    - REWD-03: Criança pode listar recompensas ativas da família (mesmo endpoint ou endpoint separado).
    - DOCS-01: Endpoints expostos via OpenAPI/Swagger.
    - Tenant isolation: todas as operações usam familyUnitId do CurrentUser.
  </behavior>
  <action>Criar DTOs: `CreateRewardRequest` (title, description, cost), `UpdateRewardRequest` (title, description, cost), `RewardResponse` (id, title, description, cost, active, createdAt). `RewardService`: `create(CurrentUser, CreateRewardRequest)`, `update(CurrentUser, UUID rewardId, UpdateRewardRequest)`, `deactivate(CurrentUser, UUID rewardId)`, `listActive(CurrentUser)`, `findById(CurrentUser, UUID rewardId)`. `RewardController`: `POST /rewards`, `PUT /rewards/{id}`, `PATCH /rewards/{id}/deactivate`, `GET /rewards` (list active), `GET /rewards/{id}`. Testes: criar recompensa, listar, editar, desativar, custo inválido, tenant isolation (família A não vê recompensas da família B).</action>
  <verify>
    <automated>cd backend && ./mvnw test</automated>
  </verify>
  <acceptance_criteria>CRUD completo de recompensas funciona com tenant isolation; testes de integração passam; Swagger expõe endpoints.</acceptance_criteria>
  <done>API de recompensas disponível para o responsável e listagem para a criança.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Children can redeem active family rewards with transactional debits and history | Schema + entities in 03-01, debit in 03-02, redemption in 03-03 |
| REQ | REWD-01, REWD-02, REWD-03 | Covered by 03-01 |
| REQ | REWD-04, REWD-05, REWD-06 | Covered by 03-03 |
| REQ | WALT-03 | Covered by 03-02 |
| REQ | WALT-05 | Covered by 03-02 |
| REQ | DOCS-01 | Covered by 03-01 initial OpenAPI, 03-03 full assertion |
| CONTEXT | D-R01..D-R10 | Covered across all plans |
| RESULT | Missing items | None |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api-to-database-schema | Services persistem dados de recompensa e resgate contra checks e constraints definidos em V3. |
| wallet-debit-boundary | Debit transacional deve decrementar balance e escrever CoinTransaction atomicamente. |
| test-cleanup-to-database | Testes truncam tabelas com FKs em ordem segura. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `rewards.cost` | mitigate | DB CHECK cost > 0, service validation |
| T-03-02 | Tampering | Wallet balance on redemption | mitigate | Pessimistic lock + balance check + DB CHECK balance_after >= 0 |
| T-03-03 | Repudiation | Redemption without CoinTransaction | mitigate | @Transactional ensures debit + CoinTransaction are atomic |
| T-03-04 | Information disclosure | Cross-family reward access | mitigate | Family-scoped queries, CurrentUser-derived familyUnitId |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
Executar `cd backend && ./mvnw -DskipTests compile` após tasks de schema/modelo. Executar `cd backend && ./mvnw test` após task de API/testes.
</verification>

<success_criteria>
Recompensas podem ser criadas, editadas, listadas e desativadas pelo responsável. Criança pode listar recompensas ativas da família. Schema de reward_redemptions está pronto para os planos dependentes.
</success_criteria>

<output>
Create `.planning/phases/03-recompensas-e-resgates/03-01-SUMMARY.md` when done.
</output>
