---
phase: 04-mobile-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - mobile/package-lock.json
  - mobile/app.json
  - mobile/tsconfig.json
  - mobile/App.tsx
  - mobile/index.ts
  - mobile/jest.setup.ts
  - mobile/src/__tests__/app-smoke.test.tsx
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02
user_setup: []
must_haves:
  truths:
    - "D-12/MOBL-01: Expo React Native TypeScript app starts from `mobile/` without Expo Router as the primary navigation model."
    - "D-13/MOBL-02: Initial visible app text is PT-BR while code identifiers remain English."
    - "D-16/MOBL-06: Scaffold preserves the Habitinhos design contract inputs and does not copy Stitch HTML/CSS."
  artifacts:
    - path: "mobile/package.json"
      provides: "Expo scripts for start, lint, typecheck, and non-watch test execution"
    - path: "mobile/App.tsx"
      provides: "Initial Expo app entry component ready for later navigation wiring"
    - path: "mobile/src/__tests__/app-smoke.test.tsx"
      provides: "First Jest/React Native Testing Library smoke test"
  key_links:
    - from: "mobile/package.json"
      to: "mobile/tsconfig.json"
      via: "`npm run typecheck` uses the scaffold TypeScript config"
      pattern: "tsc.*--noEmit"
    - from: "mobile/package.json"
      to: "mobile/src/__tests__/app-smoke.test.tsx"
      via: "`npm test -- --runInBand` executes jest-expo tests"
      pattern: "jest"
---

<objective>
Criar a base Expo + React Native + TypeScript em `mobile/` com scripts verificaveis.

Purpose: cumprir MOBL-01 e preparar um projeto mobile testavel antes de adicionar auth, API, navegacao e UI.
Output: scaffold Expo TypeScript, scripts `start`, `lint`, `typecheck`, `test`, setup de Jest e primeiro teste smoke.
</objective>

<execution_context>
@/home/dmagliano/.codex/get-shit-done/workflows/execute-plan.md
@/home/dmagliano/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DECISIONS.md
@.planning/phases/04-mobile-base/04-CONTEXT.md
@.planning/phases/04-mobile-base/04-RESEARCH.md
@.planning/phases/04-mobile-base/04-VALIDATION.md
@.planning/phases/04-mobile-base/04-UI-SPEC.md
@docs/architecture.md
@docs/design/mobile-design-contract.md
@mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 04-01-01: Criar scaffold Expo TypeScript preservando arquivos existentes</name>
  <files>mobile/package.json, mobile/package-lock.json, mobile/app.json, mobile/tsconfig.json, mobile/App.tsx, mobile/index.ts</files>
  <read_first>
    @.planning/phases/04-mobile-base/04-CONTEXT.md
    @.planning/phases/04-mobile-base/04-RESEARCH.md
    @.planning/phases/04-mobile-base/04-UI-SPEC.md
    @docs/architecture.md
    @mobile/AGENTS.md
  </read_first>
  <action>Usar `npx create-expo-app@latest` com template TypeScript blank em um diretorio temporario fora de `mobile/`, porque `mobile/` ja contem `.gitkeep` e `AGENTS.md`. Copiar para `mobile/` apenas os arquivos de projeto gerados necessarios (`package.json`, lockfile npm, `app.json`, `tsconfig.json`, `App.tsx`, entrypoint como `index.ts` quando gerado). Preservar `mobile/AGENTS.md` sem editar. Remover qualquer artefato de Expo Router ou pasta `app/` gerada por template com rotas se aparecer; D-12 exige React Navigation como modelo de navegacao da fase. Ajustar `App.tsx` para renderizar um texto PT-BR simples como `Habitinhos` e `Preparando o app da familia...` ate o plano 04-04 substituir pela navegacao. Garantir que o scaffold nao use imagens externas, fontes externas, Lottie, SVG complexo, HTML/CSS web ou codigo dos `code.html` do Stitch, per D-16.</action>
  <verify>
    <automated>cd mobile && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>`mobile/package.json` existe; `mobile/App.tsx` existe; `mobile/tsconfig.json` existe; `mobile/AGENTS.md` permanece presente e sem alteracao intencional; `npm run typecheck` sai 0; nao existe pasta `mobile/app/` usada como Expo Router.</acceptance_criteria>
  <done>Projeto Expo TypeScript compila a partir de `mobile/` com texto inicial PT-BR.</done>
</task>

<task type="auto">
  <name>Task 04-01-02: Configurar lint, typecheck e testes mobile</name>
  <files>mobile/package.json, mobile/jest.setup.ts, mobile/src/__tests__/app-smoke.test.tsx</files>
  <read_first>
    @mobile/package.json
    @mobile/App.tsx
    @.planning/phases/04-mobile-base/04-RESEARCH.md
    @.planning/phases/04-mobile-base/04-VALIDATION.md
  </read_first>
  <action>Adicionar scripts non-watch em `mobile/package.json`: `start` para Expo, `lint` para o lint suportado pelo scaffold Expo, `typecheck` como `tsc --noEmit`, e `test` como `jest`. Instalar dependencias de teste aprovadas no `04-RESEARCH.md` Package Legitimacy Audit: `jest-expo`, `jest`, `@testing-library/react-native` e, se a instalacao exigir peer dependency, `react-test-renderer` na versao compativel com o React do scaffold. Configurar Jest com preset `jest-expo` e setup em `jest.setup.ts`. Criar `mobile/src/__tests__/app-smoke.test.tsx` renderizando `App` e verificando o texto `Habitinhos`. Nao usar watch mode em nenhum comando de verificacao.</action>
  <verify>
    <automated>cd mobile && npm run lint && npm run typecheck && npm test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>`package.json` contem scripts `start`, `lint`, `typecheck`, `test`; Jest usa preset `jest-expo`; `app-smoke.test.tsx` passa com `npm test -- --runInBand`; `npm run lint` e `npm run typecheck` saem 0.</acceptance_criteria>
  <done>Infraestrutura de teste e verificacao automatica da Phase 4 esta pronta para os planos dependentes.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Expo mobile foundation runs from `mobile/` | Covered by 04-01 scaffold and scripts |
| REQ | MOBL-01 | Covered by 04-01 |
| REQ | MOBL-02 | Covered by PT-BR initial render and English technical identifiers in 04-01 |
| RESEARCH | Use Expo TypeScript, avoid Expo Router as primary navigation model | Covered by 04-01-01 |
| RESEARCH | Jest via `jest-expo` and RNTL | Covered by 04-01-02 |
| CONTEXT | D-12, D-13, D-16 | Covered by 04-01 tasks |
| CONTEXT deferred | Phase 5/6 real flows, demo polish | Excluded |
| RESULT | Missing items | None |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-machine-to-npm | Scaffold and test dependencies are downloaded from npm through Expo-compatible tooling. |
| mobile-runtime-to-user | Initial app renders user-visible PT-BR copy before auth is implemented. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Expo scaffold | mitigate | Use official `create-expo-app` package audited in RESEARCH; remove router artifacts that contradict D-12. |
| T-04-02 | Information disclosure | Initial app shell | accept | No credentials or backend data are handled in 04-01. |
| T-04-SC | Tampering | npm installs | mitigate | Use packages listed as VERIFIED in `04-RESEARCH.md` Package Legitimacy Audit and install Expo-native packages through Expo tooling. |
</threat_model>

<verification>
Run `cd mobile && npm run lint && npm run typecheck && npm test -- --runInBand`.
</verification>

<success_criteria>
Expo TypeScript app exists under `mobile/`, starts through npm script, typechecks, lints, and has a passing smoke test.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-base/04-01-SUMMARY.md` when done.
</output>
