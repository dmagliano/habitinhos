---
phase: "01-fundacao-backend"
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - backend/src/main/java/br/com/habitinhos/config/SecurityConfig.java
  - backend/src/main/java/br/com/habitinhos/auth/AuthController.java
  - backend/src/main/java/br/com/habitinhos/auth/AuthService.java
  - backend/src/main/java/br/com/habitinhos/auth/JwtService.java
  - backend/src/main/java/br/com/habitinhos/auth/CurrentUser.java
  - backend/src/main/java/br/com/habitinhos/auth/CurrentUserProvider.java
  - backend/src/main/java/br/com/habitinhos/auth/dto/RegisterRequest.java
  - backend/src/main/java/br/com/habitinhos/auth/dto/LoginRequest.java
  - backend/src/main/java/br/com/habitinhos/auth/dto/AuthResponse.java
  - backend/src/main/java/br/com/habitinhos/auth/dto/MeResponse.java
  - backend/src/test/java/br/com/habitinhos/auth/AuthIntegrationTest.java
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
user_setup: []
must_haves:
  truths:
    - Responsible adults can register with name, email, password, and family name.
    - Login returns a usable JWT for valid credentials and rejects invalid credentials.
    - `/me` returns authenticated user and family context derived from the token.
    - Protected endpoints do not accept client-provided `familyUnitId` as authorization truth.
  artifacts:
    - `backend/src/main/java/br/com/habitinhos/config/SecurityConfig.java`
    - `backend/src/main/java/br/com/habitinhos/auth/AuthService.java`
    - `backend/src/main/java/br/com/habitinhos/auth/JwtService.java`
    - `backend/src/main/java/br/com/habitinhos/auth/CurrentUser.java`
    - `backend/src/test/java/br/com/habitinhos/auth/AuthIntegrationTest.java`
  key_links:
    - `AuthService.register` saves `FamilyUnit` and `AppUser` in one transaction.
    - `JwtService` includes `user_id`, `family_unit_id`, and `role` claims.
    - `CurrentUserProvider` extracts tenant context from Spring Security/JWT.
---

<objective>
Implement initial responsible authentication and tenant context.

Purpose: make the backend able to create a responsible user, authenticate requests, and derive `familyUnitId` server-side.
Output: auth endpoints, JWT issuing/validation, `/me`, and integration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-backend/01-CONTEXT.md
@.planning/phases/01-fundacao-backend/01-RESEARCH.md
@.planning/phases/01-fundacao-backend/01-VALIDATION.md
@docs/api-contract.md
@docs/testing-strategy.md
@.planning/phases/01-fundacao-backend/01-02-SUMMARY.md
</context>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-01 | Password stored or returned in plaintext | High | Use BCrypt and never expose `passwordHash` in DTOs |
| T-02 | Client spoofs `familyUnitId` in protected operations | High | JWT claims and `CurrentUser` are the only tenant source |
| T-04 | Protected endpoints accessible without token | High | Security config protects all endpoints except auth, Swagger/OpenAPI, and health |
| T-05 | Token forgery through weak or ignored secret | High | Use Spring Security JWT decoder with configured HMAC secret |

High-severity security regressions block plan completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement register, login, token issuing, and `/me` DTOs</name>
  <files>backend/src/main/java/br/com/habitinhos/auth/AuthController.java, backend/src/main/java/br/com/habitinhos/auth/AuthService.java, backend/src/main/java/br/com/habitinhos/auth/JwtService.java, backend/src/main/java/br/com/habitinhos/auth/dto/RegisterRequest.java, backend/src/main/java/br/com/habitinhos/auth/dto/LoginRequest.java, backend/src/main/java/br/com/habitinhos/auth/dto/AuthResponse.java, backend/src/main/java/br/com/habitinhos/auth/dto/MeResponse.java</files>
  <read_first>docs/api-contract.md, .planning/phases/01-fundacao-backend/01-RESEARCH.md</read_first>
  <action>Implement `POST /auth/register`, `POST /auth/login`, and `GET /me`. Register request fields: `name`, `email`, `password`, `familyName`. Normalize email to lowercase, reject duplicate emails with `EMAIL_ALREADY_REGISTERED`, create `FamilyUnit` and active `AppUser` role `RESPONSIBLE` in one transaction, hash the password with BCrypt, and return a JWT plus user/family summary. Login validates email/password and returns `INVALID_CREDENTIALS` for invalid credentials without revealing whether email exists. `/me` returns current user id, name, email, role, family id, and family name. Do not include password hash in any response.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `AuthController.java` contains `@PostMapping("/auth/register")` or class-level `/auth` plus `@PostMapping("/register")`.
    - `AuthController.java` contains `@PostMapping("/auth/login")` or class-level `/auth` plus `@PostMapping("/login")`.
    - `AuthController.java` or a dedicated controller contains `@GetMapping("/me")`.
    - `AuthService.java` contains `BCrypt` or uses `PasswordEncoder`.
    - `AuthResponse.java` contains `token`.
  </acceptance_criteria>
  <done>Auth API compiles and returns the contract needed by mobile later.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Spring Security JWT and current-user extraction</name>
  <files>backend/src/main/java/br/com/habitinhos/config/SecurityConfig.java, backend/src/main/java/br/com/habitinhos/auth/JwtService.java, backend/src/main/java/br/com/habitinhos/auth/CurrentUser.java, backend/src/main/java/br/com/habitinhos/auth/CurrentUserProvider.java</files>
  <read_first>.planning/phases/01-fundacao-backend/01-RESEARCH.md</read_first>
  <action>Configure Spring Security so `/auth/register`, `/auth/login`, `/v3/api-docs/**`, `/swagger-ui/**`, `/swagger-ui.html`, and `/actuator/health` are public, while every other endpoint requires authentication. Use Spring Security OAuth2 Resource Server JWT validation. Implement `JwtService` with HMAC signing using configured secret and expiration. Include `user_id`, `family_unit_id`, `role`, `email`, `iat`, and `exp` in issued tokens. Implement `CurrentUser` and `CurrentUserProvider` so controllers can derive tenant context from the authenticated JWT and pass it into services.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `SecurityConfig.java` contains `oauth2ResourceServer`.
    - `SecurityConfig.java` permits `/auth/register`.
    - `SecurityConfig.java` permits `/auth/login`.
    - `JwtService.java` contains `family_unit_id`.
    - `CurrentUser.java` contains `familyUnitId`.
  </acceptance_criteria>
  <done>Authenticated requests carry server-derived family context.</done>
</task>

<task type="auto">
  <name>Task 3: Add auth integration tests</name>
  <files>backend/src/test/java/br/com/habitinhos/auth/AuthIntegrationTest.java</files>
  <read_first>.planning/phases/01-fundacao-backend/01-VALIDATION.md, docs/testing-strategy.md</read_first>
  <action>Add integration tests using MockMvc and PostgreSQL Testcontainers that prove: registration creates `FamilyUnit`; registration creates active `AppUser` with role `RESPONSIBLE`; password hash is not equal to raw password; duplicate email is rejected; login returns token for valid credentials; login rejects invalid credentials; `/me` works with returned Bearer token and returns user/family context; `/me` without token is rejected.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `AuthIntegrationTest.java` contains a test for registration creating a family.
    - `AuthIntegrationTest.java` contains a test for invalid login.
    - `AuthIntegrationTest.java` contains a test for `/me`.
    - `cd backend && ./mvnw test` exits successfully.
  </acceptance_criteria>
  <done>AUTH-01 through AUTH-05 have automated coverage, with AUTH-06 completed in the children isolation plan.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && ./mvnw test`
- [ ] `rg -n "family_unit_id|user_id|role" backend/src/main/java/br/com/habitinhos/auth`
- [ ] `rg -n "BCrypt|PasswordEncoder" backend/src/main/java/br/com/habitinhos/auth backend/src/main/java/br/com/habitinhos/config`
- [ ] Verify no response DTO exposes `passwordHash` or `accessPinHash`.
</verification>

<success_criteria>

- Responsible can register and log in.
- `/me` is protected and returns authenticated user/family context.
- JWT-derived `CurrentUser` is available for protected services.
- Invalid credentials are rejected safely.

</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-backend/01-03-SUMMARY.md`.
</output>

