---
phase: "01-fundacao-backend"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/mvnw
  - backend/mvnw.cmd
  - backend/.mvn/wrapper/maven-wrapper.properties
  - backend/src/main/java/br/com/habitinhos/HabitinhosApplication.java
  - backend/src/main/resources/application.yml
  - backend/src/test/java/br/com/habitinhos/HabitinhosApplicationTests.java
  - backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java
  - docker-compose.yml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - CHLD-01
  - CHLD-02
  - CHLD-03
  - CHLD-04
user_setup: []
must_haves:
  truths:
    - Backend project exists in `backend/` and uses Java 17 with Spring Boot 3.5.x.
    - PostgreSQL, Flyway, JPA validation, and Testcontainers dependencies are available.
    - The backend has a repeatable test command through the Maven wrapper.
  artifacts:
    - `backend/pom.xml`
    - `backend/src/main/java/br/com/habitinhos/HabitinhosApplication.java`
    - `backend/src/main/resources/application.yml`
    - `backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java`
  key_links:
    - `docker-compose.yml` exposes the same database name/user/password used by backend local defaults.
    - Testcontainers setup feeds PostgreSQL connection properties to Spring Boot tests.
---

<objective>
Create the runnable Spring Boot backend foundation under `backend/`.

Purpose: establish the build, runtime configuration, PostgreSQL/Flyway wiring, and automated-test harness before domain code is added.
Output: Maven Spring Boot project with local database configuration and a first passing test.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-backend/01-CONTEXT.md
@.planning/phases/01-fundacao-backend/01-RESEARCH.md
@.planning/phases/01-fundacao-backend/01-VALIDATION.md
@docker-compose.yml
</context>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-04 | Protected backend endpoints accidentally left open later because security/test dependencies are missing | High | Add Spring Security and security-test dependencies from the start |
| T-05 | JWT uses hard-coded production secret | High | Add config keys with env override and mark any default as local/dev-only |
| T-03 | Tenant isolation tests cannot run against PostgreSQL-compatible behavior | High | Add Testcontainers PostgreSQL infrastructure in the first plan |

High-severity security regressions block plan completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Maven Spring Boot backend</name>
  <files>backend/pom.xml, backend/mvnw, backend/mvnw.cmd, backend/.mvn/wrapper/maven-wrapper.properties, backend/src/main/java/br/com/habitinhos/HabitinhosApplication.java</files>
  <read_first>.planning/phases/01-fundacao-backend/01-RESEARCH.md</read_first>
  <action>Create a Maven-based Spring Boot project in `backend/` using group `br.com.habitinhos`, artifact `habitinhos-backend`, Java `17`, and Spring Boot `3.5.14` unless a newer `3.5.x` patch is available at execution time. Add dependencies for web, validation, data-jpa, security, oauth2-resource-server, actuator, PostgreSQL, Flyway core, Flyway PostgreSQL database support, springdoc OpenAPI WebMVC UI, Spring Boot tests, Spring Security tests, Spring Boot Testcontainers, JUnit Jupiter Testcontainers, and PostgreSQL Testcontainers. Add the Maven wrapper so the project can be tested with `./mvnw`. Create `HabitinhosApplication` in package `br.com.habitinhos`. Do not create mobile, mission, reward, dashboard, or coin-ledger code.</action>
  <verify>cd backend && ./mvnw -q -DskipTests compile</verify>
  <acceptance_criteria>
    - `backend/pom.xml` contains `spring-boot-starter-web`.
    - `backend/pom.xml` contains `spring-boot-starter-data-jpa`.
    - `backend/pom.xml` contains `spring-boot-starter-oauth2-resource-server`.
    - `backend/pom.xml` contains `org.testcontainers`.
    - `backend/src/main/java/br/com/habitinhos/HabitinhosApplication.java` contains `@SpringBootApplication`.
  </acceptance_criteria>
  <done>Maven compile succeeds and the backend scaffold is contained under `backend/`.</done>
</task>

<task type="auto">
  <name>Task 2: Configure local PostgreSQL, Flyway, JPA validation, and JWT settings</name>
  <files>backend/src/main/resources/application.yml, docker-compose.yml</files>
  <read_first>.planning/phases/01-fundacao-backend/01-CONTEXT.md, docs/testing-strategy.md</read_first>
  <action>Configure `application.yml` with local defaults for PostgreSQL: URL `jdbc:postgresql://localhost:5432/habitinhos`, username `habitinhos`, password `habitinhos`, Flyway enabled, and `spring.jpa.hibernate.ddl-auto=validate`. Add app config under `habitinhos.security.jwt` with `secret`, `issuer`, and `expiration-minutes`; default secret must be clearly local/dev-only and overrideable by env such as `HABITINHOS_JWT_SECRET`. Ensure `docker-compose.yml` has a `postgres` service using the same database, username, password, and port `5432:5432` without adding unrelated services.</action>
  <verify>rg -n "ddl-auto: validate|flyway|HABITINHOS_JWT_SECRET|POSTGRES_DB: habitinhos" backend/src/main/resources/application.yml docker-compose.yml</verify>
  <acceptance_criteria>
    - `backend/src/main/resources/application.yml` contains `ddl-auto: validate`.
    - `backend/src/main/resources/application.yml` contains `jdbc:postgresql://localhost:5432/habitinhos`.
    - `backend/src/main/resources/application.yml` contains `HABITINHOS_JWT_SECRET`.
    - `docker-compose.yml` contains `POSTGRES_DB: habitinhos`.
  </acceptance_criteria>
  <done>Local config and Docker database defaults agree.</done>
</task>

<task type="auto">
  <name>Task 3: Add PostgreSQL-backed test harness</name>
  <files>backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java, backend/src/test/java/br/com/habitinhos/HabitinhosApplicationTests.java</files>
  <read_first>.planning/phases/01-fundacao-backend/01-VALIDATION.md</read_first>
  <action>Create a shared integration-test base that starts PostgreSQL with Testcontainers and wires it into Spring Boot tests using `@ServiceConnection` if available for the selected Spring Boot version, or `@DynamicPropertySource` if simpler in the local setup. Add a context-load smoke test that uses the shared base. Keep tests independent of a locally running Docker Compose database.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java` contains `PostgreSQLContainer`.
    - `backend/src/test/java/br/com/habitinhos/HabitinhosApplicationTests.java` contains `contextLoads`.
    - `cd backend && ./mvnw test` exits successfully.
  </acceptance_criteria>
  <done>The backend has a working PostgreSQL-compatible test baseline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && ./mvnw test`
- [ ] `rg -n "spring-boot-starter-oauth2-resource-server|flyway-database-postgresql|springdoc-openapi" backend/pom.xml`
- [ ] `rg -n "ddl-auto: validate|HABITINHOS_JWT_SECRET" backend/src/main/resources/application.yml`
</verification>

<success_criteria>

- Backend scaffold exists only under `backend/`.
- Maven wrapper test command works.
- PostgreSQL/Flyway/JPA validation are configured.
- Testcontainers is ready for Phase 1 integration tests.
- No Phase 2, Phase 3, or mobile implementation appears.

</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-backend/01-01-SUMMARY.md`.
</output>

