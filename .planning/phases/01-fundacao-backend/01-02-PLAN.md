---
phase: "01-fundacao-backend"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/src/main/resources/db/migration/V1__create_foundation_schema.sql
  - backend/src/main/java/br/com/habitinhos/shared/model/BaseEntity.java
  - backend/src/main/java/br/com/habitinhos/shared/error/ApiError.java
  - backend/src/main/java/br/com/habitinhos/shared/error/GlobalExceptionHandler.java
  - backend/src/main/java/br/com/habitinhos/shared/error/NotFoundException.java
  - backend/src/main/java/br/com/habitinhos/family/FamilyUnit.java
  - backend/src/main/java/br/com/habitinhos/family/FamilyUnitRepository.java
  - backend/src/main/java/br/com/habitinhos/auth/AppUser.java
  - backend/src/main/java/br/com/habitinhos/auth/UserRole.java
  - backend/src/main/java/br/com/habitinhos/auth/AppUserRepository.java
  - backend/src/main/java/br/com/habitinhos/children/ChildProfile.java
  - backend/src/main/java/br/com/habitinhos/children/ChildProfileRepository.java
  - backend/src/main/java/br/com/habitinhos/wallet/Wallet.java
  - backend/src/main/java/br/com/habitinhos/wallet/WalletRepository.java
autonomous: true
requirements:
  - AUTH-03
  - AUTH-05
  - AUTH-06
  - CHLD-01
  - CHLD-02
  - CHLD-03
  - CHLD-04
user_setup: []
must_haves:
  truths:
    - Flyway creates Phase 1 tables for family, responsible user, child, and wallet.
    - Every Phase 1 family-scoped table stores `family_unit_id`.
    - Repositories expose family-scoped lookup methods needed for tenant isolation.
  artifacts:
    - `backend/src/main/resources/db/migration/V1__create_foundation_schema.sql`
    - `backend/src/main/java/br/com/habitinhos/auth/AppUser.java`
    - `backend/src/main/java/br/com/habitinhos/children/ChildProfile.java`
    - `backend/src/main/java/br/com/habitinhos/wallet/Wallet.java`
  key_links:
    - `wallets.child_id` references `child_profiles.id` and is unique.
    - `AppUser` maps to table `app_users`, not `user`.
    - `ChildProfileRepository` has `findByIdAndFamilyUnitId...` style methods.
---

<objective>
Create the Phase 1 persistence model and database schema.

Purpose: establish the family tenant boundary and wallet ownership model before auth and endpoints are implemented.
Output: Flyway migration, JPA entities, repositories, and shared error primitives.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-backend/01-CONTEXT.md
@.planning/phases/01-fundacao-backend/01-RESEARCH.md
@.planning/phases/01-fundacao-backend/01-PATTERNS.md
@docs/data-model.md
</context>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-02 | Client-controlled or missing tenant field enables future data leakage | High | Every family-scoped table has non-null `family_unit_id`; repositories include scoped methods |
| T-03 | Cross-family wallet/child relationships become possible | High | Foreign keys plus service-ready repository methods enforce family ownership |
| T-06 | Optional child PIN stored in plaintext | High | Store only nullable `access_pin_hash`; no plaintext PIN column |

High-severity security regressions block plan completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 1 Flyway migration</name>
  <files>backend/src/main/resources/db/migration/V1__create_foundation_schema.sql</files>
  <read_first>.planning/phases/01-fundacao-backend/01-RESEARCH.md, docs/data-model.md</read_first>
  <action>Create `V1__create_foundation_schema.sql` with tables `family_units`, `app_users`, `child_profiles`, and `wallets`. Use `UUID` primary keys, `created_at`, `updated_at`, and `active` where applicable. `app_users.email` must be globally unique and lowercased by application code. Add role check for `RESPONSIBLE` and reserved `ADMIN`. Add `wallets.balance INTEGER NOT NULL DEFAULT 0 CHECK (balance >= 0)` and `wallets.child_id UUID NOT NULL UNIQUE`. Add non-null `family_unit_id` foreign keys for `app_users`, `child_profiles`, and `wallets`. Add indexes on family and child lookup columns. Do not add mission, reward, redemption, or coin transaction tables in this migration.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - Migration contains `CREATE TABLE family_units`.
    - Migration contains `CREATE TABLE app_users`.
    - Migration contains `CREATE TABLE child_profiles`.
    - Migration contains `CREATE TABLE wallets`.
    - Migration does not contain `missions`, `rewards`, `redemptions`, or `coin_transactions`.
  </acceptance_criteria>
  <done>Flyway can create and validate the Phase 1 schema through the test harness.</done>
</task>

<task type="auto">
  <name>Task 2: Add entities and family-scoped repositories</name>
  <files>backend/src/main/java/br/com/habitinhos/family/FamilyUnit.java, backend/src/main/java/br/com/habitinhos/family/FamilyUnitRepository.java, backend/src/main/java/br/com/habitinhos/auth/AppUser.java, backend/src/main/java/br/com/habitinhos/auth/UserRole.java, backend/src/main/java/br/com/habitinhos/auth/AppUserRepository.java, backend/src/main/java/br/com/habitinhos/children/ChildProfile.java, backend/src/main/java/br/com/habitinhos/children/ChildProfileRepository.java, backend/src/main/java/br/com/habitinhos/wallet/Wallet.java, backend/src/main/java/br/com/habitinhos/wallet/WalletRepository.java</files>
  <read_first>.planning/phases/01-fundacao-backend/01-PATTERNS.md</read_first>
  <action>Implement JPA entities matching the migration. Use `AppUser` mapped to `app_users`, `UserRole` enum with `RESPONSIBLE` and `ADMIN`, and UUID ids. Keep child optional fields `age`, `avatarKey`, and `accessPinHash`. Add repository methods needed by later services: `AppUserRepository.findByEmailIgnoreCase`, `AppUserRepository.existsByEmailIgnoreCase`, `ChildProfileRepository.findAllByFamilyUnitIdAndActiveTrueOrderByCreatedAtAsc`, `ChildProfileRepository.findByIdAndFamilyUnitId`, `ChildProfileRepository.findByIdAndFamilyUnitIdAndActiveTrue`, `WalletRepository.findByChildIdAndFamilyUnitId`, and `WalletRepository.existsByChildId`. Avoid repository methods that load child records by id without family scope for controller/service usage.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `AppUser.java` contains `@Table(name = "app_users")`.
    - `ChildProfileRepository.java` contains `findByIdAndFamilyUnitId`.
    - `WalletRepository.java` contains `findByChildIdAndFamilyUnitId`.
    - `Wallet.java` contains `balance`.
  </acceptance_criteria>
  <done>Entities and repositories compile and match the Flyway schema.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal shared error and timestamp support</name>
  <files>backend/src/main/java/br/com/habitinhos/shared/model/BaseEntity.java, backend/src/main/java/br/com/habitinhos/shared/error/ApiError.java, backend/src/main/java/br/com/habitinhos/shared/error/GlobalExceptionHandler.java, backend/src/main/java/br/com/habitinhos/shared/error/NotFoundException.java</files>
  <read_first>docs/api-contract.md</read_first>
  <action>Add a small shared base/timestamp approach if useful for entities, and a simple error contract matching `ApiError { code, message, details }`. Add `NotFoundException` and `GlobalExceptionHandler` mappings for validation errors, unauthorized/forbidden defaults if naturally available, and not-found errors. Keep this minimal; do not build a large exception hierarchy.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `ApiError.java` contains `code`.
    - `ApiError.java` contains `message`.
    - `GlobalExceptionHandler.java` handles validation errors.
    - `NotFoundException.java` exists.
  </acceptance_criteria>
  <done>Shared primitives are sufficient for auth and children endpoints without overengineering.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && ./mvnw test`
- [ ] `rg -n "CREATE TABLE (family_units|app_users|child_profiles|wallets)" backend/src/main/resources/db/migration/V1__create_foundation_schema.sql`
- [ ] `rg -n "findByIdAndFamilyUnitId|findByChildIdAndFamilyUnitId" backend/src/main/java/br/com/habitinhos`
- [ ] Confirm no Phase 2/3 tables exist in migration.
</verification>

<success_criteria>

- Flyway migration owns the schema.
- Hibernate validates against the migration.
- Family-scoped repositories are available for later endpoint isolation.
- Password and child PIN storage fields are hash-only.

</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-backend/01-02-SUMMARY.md`.
</output>

