---
phase: "01-fundacao-backend"
plan: "04"
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - backend/src/main/java/br/com/habitinhos/config/OpenApiConfig.java
  - backend/src/main/java/br/com/habitinhos/children/ChildController.java
  - backend/src/main/java/br/com/habitinhos/children/ChildService.java
  - backend/src/main/java/br/com/habitinhos/children/dto/ChildRequest.java
  - backend/src/main/java/br/com/habitinhos/children/dto/ChildResponse.java
  - backend/src/main/java/br/com/habitinhos/wallet/WalletService.java
  - backend/src/test/java/br/com/habitinhos/children/ChildIntegrationTest.java
  - backend/src/test/java/br/com/habitinhos/children/TenantIsolationIntegrationTest.java
autonomous: true
requirements:
  - AUTH-05
  - AUTH-06
  - CHLD-01
  - CHLD-02
  - CHLD-03
  - CHLD-04
user_setup: []
must_haves:
  truths:
    - Responsible can create, list, view, update, and deactivate own family children.
    - Child creation creates exactly one zero-balance wallet in the same transaction.
    - Cross-family child read/update/deactivate attempts are blocked.
    - Child request/response DTOs never expose `familyUnitId`, `passwordHash`, or `accessPinHash`.
  artifacts:
    - `backend/src/main/java/br/com/habitinhos/children/ChildController.java`
    - `backend/src/main/java/br/com/habitinhos/children/ChildService.java`
    - `backend/src/main/java/br/com/habitinhos/wallet/WalletService.java`
    - `backend/src/test/java/br/com/habitinhos/children/TenantIsolationIntegrationTest.java`
  key_links:
    - `ChildController` gets `CurrentUser` from auth infrastructure and passes it to `ChildService`.
    - `ChildService.create` calls `WalletService.createForChild` inside a transaction.
    - Child repository calls include `familyUnitId` from `CurrentUser`.
---

<objective>
Implement Phase 1 children API with automatic wallet creation and tenant isolation.

Purpose: complete the backend foundation flow needed before missions, rewards, and mobile consume the API.
Output: protected children endpoints, wallet creation service, OpenAPI security setup, and cross-family tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacao-backend/01-CONTEXT.md
@.planning/phases/01-fundacao-backend/01-RESEARCH.md
@.planning/phases/01-fundacao-backend/01-VALIDATION.md
@docs/api-contract.md
@docs/testing-strategy.md
@.planning/phases/01-fundacao-backend/01-02-SUMMARY.md
@.planning/phases/01-fundacao-backend/01-03-SUMMARY.md
</context>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-02 | Client sends `familyUnitId` to create child in another family | High | Child DTO omits `familyUnitId`; service uses `CurrentUser.familyUnitId` |
| T-03 | User mutates another family's child by guessing UUID | High | Scoped repository methods and cross-family tests for read/update/deactivate |
| T-06 | Child PIN hash leaks in response | High | Response DTO excludes `accessPinHash`; request PIN is hashed before storage if provided |

High-severity security regressions block plan completion.

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement child DTOs, controller, service, and wallet creation</name>
  <files>backend/src/main/java/br/com/habitinhos/children/ChildController.java, backend/src/main/java/br/com/habitinhos/children/ChildService.java, backend/src/main/java/br/com/habitinhos/children/dto/ChildRequest.java, backend/src/main/java/br/com/habitinhos/children/dto/ChildResponse.java, backend/src/main/java/br/com/habitinhos/wallet/WalletService.java</files>
  <read_first>.planning/phases/01-fundacao-backend/01-CONTEXT.md, docs/api-contract.md</read_first>
  <action>Implement protected endpoints `POST /children`, `GET /children`, `GET /children/{id}`, `PUT /children/{id}`, and `PATCH /children/{id}/deactivate`. Request fields should include `name`, optional `age`, optional `avatarKey`, and optional `accessPin`; do not accept `familyUnitId`. Response fields should include child `id`, `name`, `age`, `avatarKey`, `active`, `createdAt`, and `updatedAt`; do not return `familyUnitId` or `accessPinHash`. In `ChildService`, require responsible role, use `CurrentUser.familyUnitId`, and use family-scoped repository methods. Create wallet through `WalletService.createForChild` with balance zero in the same transaction as child creation. Update should allow name, age, avatarKey, and optional accessPin replacement; it must not reactivate a deactivated child unless explicitly chosen and documented, so keep deactivate one-way for Phase 1.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `ChildController.java` contains `@PostMapping`.
    - `ChildController.java` contains `@GetMapping`.
    - `ChildController.java` contains `@PatchMapping`.
    - `ChildService.java` contains `familyUnitId`.
    - `WalletService.java` creates `balance` zero.
  </acceptance_criteria>
  <done>Children API works only through authenticated family context and creates wallets atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Add children and wallet integration tests</name>
  <files>backend/src/test/java/br/com/habitinhos/children/ChildIntegrationTest.java</files>
  <read_first>.planning/phases/01-fundacao-backend/01-VALIDATION.md, docs/testing-strategy.md</read_first>
  <action>Add integration tests that register/login a responsible user, create a child without child email, assert the child belongs to the authenticated family, assert exactly one wallet exists for that child with zero balance and same family, list children, get one child, update allowed child fields, and deactivate a child. Include a test that optional `avatarKey` is persisted and optional `accessPin` is hashed or at least never returned/exposed.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `ChildIntegrationTest.java` contains a test for wallet creation.
    - `ChildIntegrationTest.java` contains a test for creating child without email.
    - `ChildIntegrationTest.java` contains a test for deactivate.
    - `cd backend && ./mvnw test` exits successfully.
  </acceptance_criteria>
  <done>CHLD-01 through CHLD-04 have automated coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Add tenant isolation tests and OpenAPI bearer configuration</name>
  <files>backend/src/test/java/br/com/habitinhos/children/TenantIsolationIntegrationTest.java, backend/src/main/java/br/com/habitinhos/config/OpenApiConfig.java</files>
  <read_first>.planning/phases/01-fundacao-backend/01-RESEARCH.md, docs/api-contract.md</read_first>
  <action>Add integration tests that create two responsible users in two different families. Family A creates a child. Family B must not see that child in `GET /children`, and `GET /children/{id}`, `PUT /children/{id}`, and `PATCH /children/{id}/deactivate` using Family A's child id must be blocked with the selected safe status, preferably `404 Not Found`. Add a minimal OpenAPI config that declares Bearer JWT security for protected endpoints and leaves `/auth/register` and `/auth/login` documented as public. Do not implement wallet statement, dashboard, mission, or reward endpoints.</action>
  <verify>cd backend && ./mvnw test</verify>
  <acceptance_criteria>
    - `TenantIsolationIntegrationTest.java` creates two families.
    - `TenantIsolationIntegrationTest.java` tests cross-family `GET`.
    - `TenantIsolationIntegrationTest.java` tests cross-family `PUT`.
    - `TenantIsolationIntegrationTest.java` tests cross-family deactivate.
    - `OpenApiConfig.java` contains `bearer`.
  </acceptance_criteria>
  <done>AUTH-06 is proven by tests and Swagger is prepared for future mobile consumption.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && ./mvnw test`
- [ ] `rg -n "familyUnitId" backend/src/main/java/br/com/habitinhos/children backend/src/main/java/br/com/habitinhos/wallet`
- [ ] `rg -n "accessPinHash|passwordHash" backend/src/main/java/br/com/habitinhos/**/*Response.java` returns no exposed response field, or any hit is manually justified and removed.
- [ ] `rg -n "missions|rewards|dashboard|coin_transactions" backend/src/main/java backend/src/main/resources/db/migration` confirms no out-of-scope implementation.
</verification>

<success_criteria>

- Responsible can manage children through protected API.
- Child creation creates a wallet atomically.
- Family isolation is enforced and tested across read/update/deactivate.
- OpenAPI has Bearer JWT configuration.
- Phase 1 stays within backend foundation scope.

</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-backend/01-04-SUMMARY.md`.
</output>

