---
phase: 06-fluxo-do-respons-vel
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - backend/src/main/java/br/com/habitinhos/rewards/RewardController.java
  - backend/src/main/java/br/com/habitinhos/rewards/RewardService.java
  - backend/src/main/java/br/com/habitinhos/rewards/RewardRepository.java
  - backend/src/test/java/br/com/habitinhos/rewards/RewardIntegrationTest.java
  - mobile/src/navigation/routes.ts
  - mobile/src/features/responsible/responsibleService.ts
  - mobile/src/features/responsible/ResponsibleRewardsScreen.tsx
  - mobile/src/features/responsible/ResponsibleRewardFormScreen.tsx
  - mobile/src/features/responsible/ResponsibleProfileScreen.tsx
  - mobile/src/features/responsible/components/ManageListItem.tsx
  - mobile/src/features/responsible/components/ResponsibleFormSection.tsx
  - mobile/src/features/responsible/components/CoinValueControl.tsx
  - mobile/src/features/responsible/components/EmojiPicker.tsx
  - mobile/src/features/responsible/components/ConfirmActionSheet.tsx
  - mobile/src/features/responsible/__tests__/responsible-rewards.test.tsx
  - mobile/src/features/responsible/__tests__/responsible-navigation.test.tsx
autonomous: true
requirements:
  - MOBL-03
  - REWD-01
user_setup:
  - "Manual Expo smoke at Android 360px-430px after automated checks."

must_haves:
  truths:
    - "D-20/D-21/D-22: Reward management uses Expo React Native TypeScript and real backend reward endpoints only."
    - "D-23: Rewards/profile screens follow 06-UI-SPEC, mobile-design-contract, and phase-design-map."
    - "D-24: Reward deactivation is explained as preserving history and child redemption records."
  artifacts:
    - path: "mobile/src/features/responsible/ResponsibleRewardsScreen.tsx"
      provides: "Responsible reward management list"
    - path: "mobile/src/features/responsible/ResponsibleRewardFormScreen.tsx"
      provides: "Create/edit/deactivate reward flow"
    - path: "mobile/src/features/responsible/ResponsibleProfileScreen.tsx"
      provides: "Mode switching and logout surface for responsible tabs"
---

<objective>
Implementar gestao de recompensas e finalizar a superficie do responsavel.

Purpose: permitir que o responsavel crie, edite, liste e desative recompensas, com perfil/troca de modo e validacao final da Fase 6.
Output: telas de recompensas, formulario, perfil responsavel, testes `responsible-rewards` e smoke final orientado pela UI-SPEC.
</objective>

<execution_context>
@/home/dmagliano/.codex/get-shit-done/workflows/execute-plan.md
@/home/dmagliano/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-fluxo-do-respons-vel/06-CONTEXT.md
@.planning/phases/06-fluxo-do-respons-vel/06-RESEARCH.md
@.planning/phases/06-fluxo-do-respons-vel/06-VALIDATION.md
@.planning/phases/06-fluxo-do-respons-vel/06-PATTERNS.md
@.planning/phases/06-fluxo-do-respons-vel/06-UI-SPEC.md
@.planning/phases/06-fluxo-do-respons-vel/06-01-SUMMARY.md
@.planning/phases/06-fluxo-do-respons-vel/06-02-SUMMARY.md
@.planning/phases/06-fluxo-do-respons-vel/06-03-SUMMARY.md
@mobile/AGENTS.md
@mobile/src/features/responsible/responsibleService.ts
@mobile/src/features/responsible/ResponsibleTabsScreen.tsx
@mobile/src/features/child/ChildRewardsScreen.tsx
@mobile/src/features/child/components/RewardCard.tsx
@mobile/src/features/auth/AuthContext.tsx

<interfaces>
Reward endpoints:
- GET /rewards?includeInactive=true
- POST /rewards
- GET /rewards/{id}
- PUT /rewards/{id}
- PATCH /rewards/{id}/deactivate

Profile actions:
- `Voltar para seleção de modo` navigates to FamilyHub.
- `Sair da conta` calls AuthContext logout.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | MOBL-03 | Responsible flow includes rewards and profile/mode actions | 06-04 | COVERED | This plan completes reward/profile surfaces.
REQ | REWD-01 | Responsible adult can create, edit, list, view, and deactivate rewards | 06-04 | COVERED | Reward list/form/deactivation.
CONTEXT | D-20..D-24 | Stack, backend truth, real API, UI spec, soft deactivation | 06-04 | COVERED | Must-haves and tasks enforce these.
UI-SPEC | Reward management | Use active/inactive status, cost, emoji, form, and deactivation confirmation | 06-04 | COVERED | Tests and manual smoke verify.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inactive reward list backend support</name>
  <read_first>
    - backend/src/main/java/br/com/habitinhos/rewards/RewardController.java
    - backend/src/main/java/br/com/habitinhos/rewards/RewardService.java
    - backend/src/main/java/br/com/habitinhos/rewards/RewardRepository.java
    - backend/src/test/java/br/com/habitinhos/rewards/RewardIntegrationTest.java
    - .planning/phases/06-fluxo-do-respons-vel/06-RESEARCH.md
  </read_first>
  <files>RewardController.java, RewardService.java, RewardRepository.java, RewardIntegrationTest.java</files>
  <behavior>
    - Test first: GET /rewards remains active-only by default.
    - Test first: GET /rewards?includeInactive=true returns active and inactive rewards from the authenticated family.
    - Test first: includeInactive never returns rewards from another family.
  </behavior>
  <action>Extend `RewardController.listActive` or rename internally to list with optional `includeInactive=false` while keeping the route `GET /rewards`. Add service/repository support for family-scoped all-reward listing and preserve active-only default for the child catalog.</action>
  <verify>
    <automated>cd backend && ./mvnw test -Dtest=RewardIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - Existing active-only reward list tests still pass.
    - New includeInactive tests prove inactive visibility and family isolation.
  </acceptance_criteria>
  <done>Backend supports responsible reward management lists without breaking child catalog defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reward service methods and management list</name>
  <read_first>
    - mobile/src/features/responsible/responsibleService.ts
    - mobile/src/api/types.ts
    - backend/src/main/java/br/com/habitinhos/rewards/dto/CreateRewardRequest.java
    - backend/src/main/java/br/com/habitinhos/rewards/dto/UpdateRewardRequest.java
    - mobile/src/features/child/ChildRewardsScreen.tsx
    - mobile/src/features/child/components/RewardCard.tsx
    - .planning/phases/06-fluxo-do-respons-vel/06-UI-SPEC.md
  </read_first>
  <files>responsibleService.ts, ResponsibleRewardsScreen.tsx, ManageListItem.tsx, responsible-rewards.test.tsx</files>
  <behavior>
    - Test first: reward screen calls `listRewards(token, true)`.
    - Test first: active rewards appear before inactive rewards.
    - Test first: list item shows title, description/cost, `Ativa`/`Inativa`, and `Editar`.
    - Test first: empty state renders `Nenhuma recompensa cadastrada` and CTA `Nova recompensa`.
  </behavior>
  <action>Add `createReward`, `getReward`, `updateReward`, and `deactivateReward` methods to `responsibleService` using backend DTO field names from reward request records. Implement `ResponsibleRewardsScreen` with header `Recompensas`, CTA `Nova recompensa`, active/inactive sections, `CoinBadge`/cost display, and management item actions. Use `GET /rewards?includeInactive=true`; do not alter the child reward catalog's active-only expectation.</action>
  <verify>
    <automated>cd mobile && npm test -- responsible-rewards --runInBand</automated>
    <automated>cd mobile && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Reward management list includes inactive records only through responsible service.
    - No reward item uses fake availability or local redemption state.
    - Service tests prove no `familyUnitId` route/body payloads.
  </acceptance_criteria>
  <done>Responsible can browse active and inactive family rewards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build create/edit/deactivate reward form</name>
  <read_first>
    - mobile/src/features/responsible/ResponsibleRewardsScreen.tsx
    - mobile/src/features/responsible/responsibleService.ts
    - mobile/src/features/responsible/components/ResponsibleFormSection.tsx
    - mobile/src/features/responsible/components/CoinValueControl.tsx
    - mobile/src/features/responsible/components/EmojiPicker.tsx
    - mobile/src/features/responsible/components/ConfirmActionSheet.tsx
    - .planning/phases/06-fluxo-do-respons-vel/06-UI-SPEC.md
  </read_first>
  <files>ResponsibleRewardFormScreen.tsx, CoinValueControl.tsx, EmojiPicker.tsx, ConfirmActionSheet.tsx, responsible-rewards.test.tsx</files>
  <behavior>
    - Test first: create reward submits POST /rewards with title, description, cost, and emoji/avatar key if supported by current DTO.
    - Test first: edit reward loads GET /rewards/{id} and submits PUT /rewards/{id}.
    - Test first: cost must be a positive integer before submit.
    - Test first: `Desativar recompensa` opens confirmation explaining history is preserved.
    - Test first: deactivation calls PATCH /rewards/{id}/deactivate and refetches list.
  </behavior>
  <action>Implement `ResponsibleRewardFormScreen` with fields `Nome da recompensa`, `Descricao`, `Custo em moedas`, and optional emoji/category only if supported by the current mobile/backend DTOs; otherwise do not invent unsupported backend fields. `CoinValueControl` must enforce minimum 1. Use confirmation copy `A recompensa deixa de aparecer para as crianças, mas resgates anteriores continuam no histórico.`. Disable duplicate submits and show backend-tied feedback banners after create/update/deactivate.</action>
  <verify>
    <automated>cd mobile && npm test -- responsible-rewards --runInBand</automated>
    <automated>cd mobile && npm run typecheck</automated>
    <automated>cd mobile && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Reward form sends only backend-supported fields.
    - Deactivation uses PATCH, never DELETE.
    - Form visible copy is PT-BR and fits single-column Android layout.
  </acceptance_criteria>
  <done>Responsible can create, edit, and deactivate rewards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Complete responsible profile and final Phase 6 validation</name>
  <read_first>
    - mobile/src/features/responsible/ResponsibleTabsScreen.tsx
    - mobile/src/features/family/FamilyHubScreen.tsx
    - mobile/src/features/auth/AuthContext.tsx
    - mobile/src/features/child/ChildTabsScreen.tsx
    - .planning/phases/06-fluxo-do-respons-vel/06-VALIDATION.md
    - .planning/phases/06-fluxo-do-respons-vel/06-UI-SPEC.md
    - docs/design/mobile-design-contract.md
    - docs/design/phase-design-map.md
  </read_first>
  <files>ResponsibleProfileScreen.tsx, ResponsibleTabsScreen.tsx, responsible-navigation.test.tsx, responsible-rewards.test.tsx</files>
  <behavior>
    - Test first: profile tab renders responsible/family identity without token/familyUnitId leakage.
    - Test first: `Trocar modo` navigates to FamilyHub.
    - Test first: `Sair da conta` calls logout.
    - Test first: all responsible tab labels remain reachable after reward implementation.
  </behavior>
  <action>Implement `ResponsibleProfileScreen` with PT-BR copy, `Trocar modo`, and `Sair da conta`, reusing existing auth context and family hub navigation. Replace any honest placeholders left in `ResponsibleTabsScreen` for rewards/profile with real screens. Run the full Phase 6 mobile validation set after tasks: responsible service, navigation, dashboard, children, missions, approvals, rewards, typecheck, and lint. Record any visual/manual Expo smoke note in the plan summary; do not create Phase 7 polish work here.</action>
  <verify>
    <automated>cd mobile && npm test -- responsible-service --runInBand</automated>
    <automated>cd mobile && npm test -- responsible-navigation --runInBand</automated>
    <automated>cd mobile && npm test -- responsible-dashboard --runInBand</automated>
    <automated>cd mobile && npm test -- responsible-children --runInBand</automated>
    <automated>cd mobile && npm test -- responsible-missions --runInBand</automated>
    <automated>cd mobile && npm test -- responsible-approvals --runInBand</automated>
    <automated>cd mobile && npm test -- responsible-rewards --runInBand</automated>
    <automated>cd mobile && npm run typecheck</automated>
    <automated>cd mobile && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Profile screen does not render JWT, token, familyUnitId, accessPin, or accessPinHash.
    - FamilyHub remains the mode switch surface.
    - Final validation commands listed in 06-VALIDATION are attempted and summarized.
  </acceptance_criteria>
  <done>Responsible rewards/profile surfaces are complete and Phase 6 mobile validation has been run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reward form -> Spring API | Reward request fields and responsible JWT cross into reward endpoints. |
| profile actions -> auth/session | Profile screen can switch mode or end the authenticated session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | reward cost | mitigate | Client validates positive integer; backend remains authoritative; tests lock payload shape. |
| T-06-04-02 | Elevation of Privilege | reward management service | mitigate | Service never accepts familyUnitId and uses Bearer token through apiRequest. |
| T-06-04-03 | Repudiation | reward deactivation | mitigate | UI copy says deactivate, not delete; backend response/refetch confirms state. |
| T-06-04-04 | Information Disclosure | responsible profile | mitigate | Profile tests assert sensitive auth/family fields are not rendered. |
</threat_model>

<verification>
Run these commands after all tasks in this plan:
- `cd backend && ./mvnw test -Dtest=RewardIntegrationTest`
- `cd mobile && npm test -- responsible-rewards --runInBand`
- `cd mobile && npm test -- responsible-navigation --runInBand`
- `cd mobile && npm test -- responsible-service --runInBand`
- `cd mobile && npm test -- responsible-dashboard --runInBand`
- `cd mobile && npm test -- responsible-children --runInBand`
- `cd mobile && npm test -- responsible-missions --runInBand`
- `cd mobile && npm test -- responsible-approvals --runInBand`
- `cd mobile && npm run typecheck`
- `cd mobile && npm run lint`
</verification>

<success_criteria>
- Responsible can create, edit, list, view, and deactivate rewards from mobile.
- Responsible profile supports mode switching and logout without leaking sensitive fields.
- REWD-01 and remaining MOBL-03 reward/profile surfaces are covered by tests.
- Final Phase 6 mobile validation commands are attempted before summary.
</success_criteria>

<output>
Create `.planning/phases/06-fluxo-do-respons-vel/06-04-SUMMARY.md` when done.
</output>
