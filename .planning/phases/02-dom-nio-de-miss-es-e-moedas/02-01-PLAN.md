---
phase: 02-dom-nio-de-miss-es-e-moedas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/resources/db/migration/V2__create_mission_coin_schema.sql
  - backend/src/main/java/br/com/habitinhos/missions/Mission.java
  - backend/src/main/java/br/com/habitinhos/missions/AssignedMission.java
  - backend/src/main/java/br/com/habitinhos/missions/RecurrenceType.java
  - backend/src/main/java/br/com/habitinhos/missions/AssignedMissionStatus.java
  - backend/src/main/java/br/com/habitinhos/missions/MissionRepository.java
  - backend/src/main/java/br/com/habitinhos/missions/AssignedMissionRepository.java
  - backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java
autonomous: true
requirements:
  - MISS-01
  - MISS-02
  - MISS-03
  - WALT-04
  - WALT-06
user_setup: []
must_haves:
  truths:
    - "D-01 Mission é template reutilizável, family-scoped, criado por responsável autenticado."
    - "D-02 AssignedMission preserva snapshot parcial de título, descrição, moedas e requiresApproval no momento da atribuição."
    - "D-03 e D-04 recurrenceType existe apenas como metadata inativa; nenhuma atribuição automática recorrente é criada."
    - "D-05 o banco e o repositório oferecem bloqueio para duplicidade ativa da mesma missão para a mesma criança."
    - "D-06 Mission contém title, description, coinValue, requiresApproval, recurrenceType, active, createdByUserId e timestamps."
    - "D-07 AssignedMission contém missionId, childId, status, dueDate opcional, timestamps de workflow, rejectionReason opcional e snapshot fields."
    - "D-12 e D-13 são preservadas no schema: moedas positivas e ledger `coin_transactions` pronto para escrita transacional."
    - "D-15 entidades backend tocadas usam Lombok para getters e construtor JPA protegido, sem `@Setter` amplo."
  artifacts:
    - path: "backend/pom.xml"
      provides: "Dependência Lombok opcional e excluída do artefato runtime"
    - path: "backend/src/main/resources/db/migration/V2__create_mission_coin_schema.sql"
      provides: "Tabelas missions, assigned_missions e coin_transactions com checks, FKs, índices e constraints de crédito duplicado"
    - path: "backend/src/main/java/br/com/habitinhos/missions/Mission.java"
      provides: "Entidade JPA de template de missão"
    - path: "backend/src/main/java/br/com/habitinhos/missions/AssignedMission.java"
      provides: "Entidade JPA de atribuição com snapshot e state fields"
    - path: "backend/src/main/java/br/com/habitinhos/missions/AssignedMissionRepository.java"
      provides: "Queries family-scoped e duplicate-open assignment guard"
  key_links:
    - from: "V2__create_mission_coin_schema.sql"
      to: "WalletService.creditForMission in 02-03"
      via: "coin_transactions schema contract"
      pattern: "uk_coin_transactions_mission_credit"
    - from: "AssignedMissionRepository"
      to: "AssignedMissionService in 02-04/02-02"
      via: "exists open assignment for missionId + childId"
      pattern: "missionId.*childId|childId.*missionId"
    - from: "AbstractIntegrationTest"
      to: "Phase 2 integration tests"
      via: "cleanup includes coin_transactions before assigned_missions before missions"
      pattern: "coin_transactions"
---

<objective>
Criar o contrato persistente de missões, atribuições e ledger de moedas antes de qualquer API.

Purpose: dar aos planos 02-04, 02-03 e 02-02 um schema concreto para MISS-01..MISS-03, WALT-04 e WALT-06, incluindo a prevenção de crédito duplicado exigida por D-13.
Output: migration V2, entidades, enums, repositórios e limpeza de testes para as tabelas Phase 2.
</objective>

<execution_context>
@/home/dmagliano/.codex/get-shit-done/workflows/execute-plan.md
@/home/dmagliano/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/DECISIONS.md
@.planning/phases/02-dom-nio-de-miss-es-e-moedas/02-CONTEXT.md
@.planning/phases/02-dom-nio-de-miss-es-e-moedas/02-RESEARCH.md
@.planning/phases/02-dom-nio-de-miss-es-e-moedas/02-VALIDATION.md
@.planning/phases/02-dom-nio-de-miss-es-e-moedas/02-PATTERNS.md
@.planning/phases/01-fundacao-backend/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-01-01: Criar migration V2 com contrato completo de missão e ledger</name>
  <files>backend/src/main/resources/db/migration/V2__create_mission_coin_schema.sql, backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java</files>
  <read_first>
    @backend/src/main/resources/db/migration/V1__create_foundation_schema.sql
    @backend/src/test/java/br/com/habitinhos/shared/AbstractIntegrationTest.java
    @.planning/phases/02-dom-nio-de-miss-es-e-moedas/02-CONTEXT.md
    @.planning/phases/02-dom-nio-de-miss-es-e-moedas/02-RESEARCH.md
  </read_first>
  <behavior>
    - D-01/D-06/MISS-01: `missions` armazena templates family-scoped com `title`, `description`, `coin_value`, `requires_approval`, `recurrence_type`, `active`, `created_by_user_id`, `created_at`, `updated_at`.
    - D-02/D-07/MISS-03: `assigned_missions` armazena `mission_id`, `child_id`, `status`, due/timestamps, rejection reason e snapshot de display/moeda/aprovação.
    - D-05: existe índice único parcial que bloqueia assignment aberto duplicado para `(mission_id, child_id)` quando status está em `PENDING` ou `AWAITING_APPROVAL`.
    - D-13/WALT-04/WALT-06: `coin_transactions` tem contrato concreto para crédito de missão e índice único parcial que bloqueia duplicate mission credit por `assigned_mission_id`.
  </behavior>
  <action>Criar `V2__create_mission_coin_schema.sql` sem editar V1. `missions`: `id UUID PRIMARY KEY`, `family_unit_id UUID NOT NULL REFERENCES family_units(id)`, `title VARCHAR(160) NOT NULL`, `description VARCHAR(1000)`, `coin_value INTEGER NOT NULL CHECK (coin_value > 0)`, `requires_approval BOOLEAN NOT NULL DEFAULT TRUE`, `recurrence_type VARCHAR(24) NOT NULL DEFAULT 'ONCE' CHECK (recurrence_type IN ('ONCE','DAILY','WEEKLY','CUSTOM'))`, `active BOOLEAN NOT NULL DEFAULT TRUE`, `created_by_user_id UUID NOT NULL REFERENCES app_users(id)`, `created_at TIMESTAMPTZ NOT NULL`, `updated_at TIMESTAMPTZ NOT NULL`, mais índices por `family_unit_id`, `(family_unit_id, active)` e `created_by_user_id`. `assigned_missions`: `id UUID PRIMARY KEY`, `family_unit_id UUID NOT NULL REFERENCES family_units(id)`, `mission_id UUID NOT NULL REFERENCES missions(id)`, `child_id UUID NOT NULL REFERENCES child_profiles(id)`, `status VARCHAR(32) NOT NULL DEFAULT 'PENDING' CHECK (status IN ('PENDING','AWAITING_APPROVAL','COMPLETED','REJECTED','CANCELLED'))`, `due_date DATE`, `completed_at TIMESTAMPTZ`, `approved_at TIMESTAMPTZ`, `rejected_at TIMESTAMPTZ`, `rejection_reason VARCHAR(500)`, `snapshot_title VARCHAR(160) NOT NULL`, `snapshot_description VARCHAR(1000)`, `snapshot_coin_value INTEGER NOT NULL CHECK (snapshot_coin_value > 0)`, `snapshot_requires_approval BOOLEAN NOT NULL`, `created_at TIMESTAMPTZ NOT NULL`, `updated_at TIMESTAMPTZ NOT NULL`, índices por família/criança/status, família/status e missão. Criar `CREATE UNIQUE INDEX uk_assigned_missions_open_mission_child ON assigned_missions(mission_id, child_id) WHERE status IN ('PENDING','AWAITING_APPROVAL')`. `coin_transactions`: `id UUID PRIMARY KEY`, `family_unit_id UUID NOT NULL REFERENCES family_units(id)`, `wallet_id UUID NOT NULL REFERENCES wallets(id)`, `child_id UUID NOT NULL REFERENCES child_profiles(id)`, `assigned_mission_id UUID REFERENCES assigned_missions(id)`, `reward_redemption_id UUID`, `type VARCHAR(20) NOT NULL CHECK (type IN ('CREDIT','DEBIT','ADJUSTMENT'))`, `source_type VARCHAR(40) NOT NULL CHECK (source_type IN ('MISSION_COMPLETION','REWARD_REDEMPTION','MANUAL_ADJUSTMENT'))`, `amount INTEGER NOT NULL CHECK (amount > 0)`, `balance_after INTEGER NOT NULL CHECK (balance_after >= 0)`, `description VARCHAR(255)`, `created_by_user_id UUID REFERENCES app_users(id)`, `created_at TIMESTAMPTZ NOT NULL`; índices por `family_unit_id`, `wallet_id`, `child_id`, `assigned_mission_id`, `created_at`; e `CREATE UNIQUE INDEX uk_coin_transactions_mission_credit ON coin_transactions(assigned_mission_id) WHERE assigned_mission_id IS NOT NULL AND type = 'CREDIT' AND source_type = 'MISSION_COMPLETION'`. Atualizar `AbstractIntegrationTest.cleanDatabase()` para truncar `coin_transactions`, `assigned_missions`, `missions`, `wallets`, `child_profiles`, `app_users`, `family_units` em ordem segura.</action>
  <verify>
    <automated>cd backend && ./mvnw -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>Migration define colunas, checks, FKs, índices, unique open-assignment e unique mission-credit exatamente como contrato acima; cleanup inclui tabelas Phase 2 antes das tabelas parent.</acceptance_criteria>
  <done>Contrato concreto de `coin_transactions` e das tabelas de missão existe para os planos dependentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-01-02: Implementar entidades, enums e repositórios de missão</name>
  <files>backend/pom.xml, backend/src/main/java/br/com/habitinhos/missions/Mission.java, backend/src/main/java/br/com/habitinhos/missions/AssignedMission.java, backend/src/main/java/br/com/habitinhos/missions/RecurrenceType.java, backend/src/main/java/br/com/habitinhos/missions/AssignedMissionStatus.java, backend/src/main/java/br/com/habitinhos/missions/MissionRepository.java, backend/src/main/java/br/com/habitinhos/missions/AssignedMissionRepository.java</files>
  <read_first>
    @backend/pom.xml
    @backend/src/main/java/br/com/habitinhos/shared/model/BaseEntity.java
    @backend/src/main/java/br/com/habitinhos/children/ChildProfile.java
    @backend/src/main/java/br/com/habitinhos/children/ChildProfileRepository.java
    @backend/src/main/java/br/com/habitinhos/wallet/Wallet.java
    @.planning/phases/02-dom-nio-de-miss-es-e-moedas/02-PATTERNS.md
  </read_first>
  <behavior>
    - D-03/D-04: `RecurrenceType` é persistido como enum metadata e não dispara nenhuma criação automática.
    - D-05: `AssignedMissionRepository` expõe método de existência para assignments abertos por `missionId + childId`.
    - D-10/D-11: repositories têm métodos `findByIdAndFamilyUnitId` e listagens family-scoped.
    - D-14: `Mission` expõe soft deactivation por `active=false`, sem operação de delete no domínio.
  </behavior>
  <action>Adicionar `org.projectlombok:lombok` em `backend/pom.xml` com `<optional>true</optional>` e excluir `lombok` do `spring-boot-maven-plugin` para não empacotar no runtime. Normalizar entidades backend tocadas da Phase 1 para o mesmo padrão de boilerplate (`@Getter` e `@NoArgsConstructor(access = AccessLevel.PROTECTED)`) sem `@Setter` amplo; quando já houver mutação por serviço, mover para métodos de domínio explícitos, como `ChildProfile.updateProfile`, `updateAccessPinHash` e `deactivate`. Criar package `br.com.habitinhos.missions`. `RecurrenceType` deve conter `ONCE`, `DAILY`, `WEEKLY`, `CUSTOM`; `AssignedMissionStatus` deve conter `PENDING`, `AWAITING_APPROVAL`, `COMPLETED`, `REJECTED`, `CANCELLED`. `Mission` e `AssignedMission` devem seguir o padrão de entidades Phase 1 com `BaseEntity`, mas usar Lombok para boilerplate: `@Getter` e `@NoArgsConstructor(access = AccessLevel.PROTECTED)` no lugar de getters manuais e construtor JPA manual. Não adicionar `@Setter` amplo; alterações de estado devem ficar em métodos de domínio para update/deactivate, snapshot, completion timestamps, approval/rejection timestamps e status. `MissionRepository` deve oferecer `findAllByFamilyUnitIdAndActiveTrueOrderByCreatedAtAsc`, `findByIdAndFamilyUnitId`, `findByIdAndFamilyUnitIdAndActiveTrue`. `AssignedMissionRepository` deve oferecer `findByIdAndFamilyUnitId`, `findAllByFamilyUnitIdAndChildIdAndStatusOrderByDueDateAscCreatedAtAsc`, `findAllByFamilyUnitIdAndStatusOrderByCompletedAtAsc`, e `existsByMissionIdAndChildIdAndStatusIn` ou query equivalente para `PENDING`/`AWAITING_APPROVAL`.</action>
  <verify>
    <automated>cd backend && ./mvnw -DskipTests compile</automated>
  </verify>
  <acceptance_criteria>`backend/pom.xml` contém Lombok opcional e excluído do plugin de runtime; entidades backend tocadas, incluindo analogs da Phase 1, usam `@Getter` e `@NoArgsConstructor(access = AccessLevel.PROTECTED)` quando aplicável, não contêm blocos de getters/setters manuais nem `@Setter` amplo; `ChildService` usa métodos de domínio em `ChildProfile`; entidades compilam contra o schema V2, enums correspondem aos checks SQL e repositórios dão os contracts consumidos por 02-04/02-02.</acceptance_criteria>
  <done>Modelo persistente de missão e atribuição existe sem API e sem lógica de carteira.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Mission assignment and completion flow can credit coins through an auditable transaction ledger | Covered by 02-01 schema, 02-04 mission/assignment API, 02-03 wallet ledger, 02-02 completion/approval/rejection |
| REQ | MISS-01, MISS-02, MISS-03 | Covered by 02-01 and 02-04 |
| REQ | MISS-04, MISS-05, MISS-06, MISS-07, MISS-08, MISS-09 | Covered by 02-02 with dependency on 02-03 for credits |
| REQ | WALT-01, WALT-02, WALT-04, WALT-06 | Covered by 02-01 schema contract, 02-03 wallet/ledger service, and 02-02 transition integration |
| REQ | DOCS-01 | Covered by 02-04 initial mission/assignment OpenAPI checks, 02-03 wallet path check, and 02-02 full Phase 2 OpenAPI path assertion |
| RESEARCH | Pessimistic wallet lock and transactional `creditForMission` | Covered by 02-03 |
| RESEARCH | Lombok added as compile-time boilerplate helper; springdoc/OpenAPI through existing config | Covered by 02-01 and all later entity plans |
| CONTEXT | D-01..D-07 mission/assignment decisions | Covered by 02-01 and 02-04 |
| CONTEXT | D-08..D-15 backend source of truth, tenant scope, wallet integrity, soft deactivation, Lombok boilerplate | Covered by 02-01, 02-04, 02-03, and 02-02 |
| CONTEXT deferred | Recurring generation, rewards, redemptions, wallet statements, mobile/dashboard | Excluded from Phase 2 plans per deferred scope |
| RESULT | Missing items | None |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api-to-database-schema | Services futuros persistirão dados financeiros e de missão contra checks e constraints definidos aqui. |
| test-cleanup-to-database | Testes truncam tabelas com FKs e precisam não deixar resíduos cross-test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `missions.coin_value`, `assigned_missions.snapshot_coin_value`, `coin_transactions.amount` | mitigate | DB `CHECK > 0` em todos os valores de moedas, per D-12. |
| T-02-02 | Tampering | `assigned_missions` | mitigate | Índice único parcial para impedir assignment aberto duplicado por missão/criança, per D-05. |
| T-02-03 | Tampering / Repudiation | `coin_transactions` | mitigate | Schema exige ledger com wallet, child, family, amount, balance_after, source/type e unique mission-credit por `assigned_mission_id`, per D-13. |
| T-02-04 | Information disclosure | Repositories | mitigate | Métodos family-scoped são o contrato padrão consumido por services; nenhum método de API deve buscar sem `familyUnitId`. |
| T-02-SC | Supply chain / Tampering | Lombok dependency | mitigate | Usar `org.projectlombok:lombok` gerenciado pelo Spring Boot, `<optional>true</optional>`, excluído do Spring Boot runtime artifact, sem plugins ou dependências adicionais. |
</threat_model>

<verification>
Executar `cd backend && ./mvnw -DskipTests compile` para compilação/migration context quando a implementação existir. Após a wave, executar `cd backend && ./mvnw test`.
</verification>

<success_criteria>
Planos dependentes recebem um contrato de banco estável: tabelas de missão, atribuição e ledger existem com colunas, checks, FKs, índices e constraints necessárias para duplicate assignment e duplicate mission credit prevention.
</success_criteria>

<output>
Create `.planning/phases/02-dom-nio-de-miss-es-e-moedas/02-01-SUMMARY.md` when done.
</output>
