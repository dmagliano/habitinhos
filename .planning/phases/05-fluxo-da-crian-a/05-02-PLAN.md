---
phase: 05-fluxo-da-crian-a
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - mobile/src/navigation/routes.ts
  - mobile/src/navigation/RootNavigator.tsx
  - mobile/src/features/child/ChildMissionsScreen.tsx
  - mobile/src/features/child/ChildMissionDetailScreen.tsx
  - mobile/src/features/child/components/MissionCard.tsx
  - mobile/src/features/child/components/ProgressBar.tsx
  - mobile/src/features/child/__tests__/child-missions.test.tsx
autonomous: true
requirements:
  - MOBL-04
  - MOBL-05
  - MISS-04
  - MISS-05
  - WALT-01
user_setup: []

must_haves:
  truths:
    - "D-09: Approval-required mission completion shows Aguardando aprovacao style feedback and does not imply coins were credited."
    - "D-10: Auto-credit mission completion shows received coin amount and refreshed backend balance."
    - "D-11: Completion feedback uses stable banners/cards, with no animation requirement."
    - "D-12: Final mission status and wallet balance come from the completion response plus backend refetch."
    - "D-18/D-19: Mission status transitions and wallet credit remain backend source-of-truth with no mock/offline fallback."
  artifacts:
    - path: "mobile/src/features/child/ChildMissionsScreen.tsx"
      provides: "Pending mission list, completion action, refetch, and feedback"
    - path: "mobile/src/features/child/ChildMissionDetailScreen.tsx"
      provides: "Mission detail and completion action"
    - path: "mobile/src/features/child/components/MissionCard.tsx"
      provides: "Reusable mission card with status, reward, and action"
    - path: "mobile/src/features/child/__tests__/child-missions.test.tsx"
      provides: "Wave 0 mission list/completion feedback tests"
  key_links:
    - from: "mobile/src/features/child/ChildMissionsScreen.tsx"
      to: "childService.listPendingMissions"
      via: "load/refetch selected child missions"
      pattern: "childService\\.listPendingMissions"
    - from: "mobile/src/features/child/ChildMissionsScreen.tsx"
      to: "childService.completeMission"
      via: "complete selected assigned mission"
      pattern: "childService\\.completeMission"
    - from: "mobile/src/features/child/ChildMissionDetailScreen.tsx"
      to: "RootStackParamList.ChildMissionDetail"
      via: "route params"
      pattern: "ChildMissionDetail"
---

<objective>
Implement the child mission loop: pending mission list, detail route, complete action, duplicate-submit protection, backend-tied feedback, and refetch of wallet/list.

Purpose: Satisfy MISS-04 and MISS-05 in the mobile child flow while honoring D-09, D-10, D-11, D-12, D-18, D-19, and D-20.
Output: Mission components/screens and `child-missions` Wave 0 tests.
</objective>

<execution_context>
@/home/dmagliano/.codex/get-shit-done/workflows/execute-plan.md
@/home/dmagliano/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fluxo-da-crian-a/05-CONTEXT.md
@.planning/phases/05-fluxo-da-crian-a/05-RESEARCH.md
@.planning/phases/05-fluxo-da-crian-a/05-VALIDATION.md
@.planning/phases/05-fluxo-da-crian-a/05-PATTERNS.md
@.planning/phases/05-fluxo-da-crian-a/05-UI-SPEC.md
@.planning/phases/05-fluxo-da-crian-a/05-01-SUMMARY.md
@mobile/src/features/child/childService.ts
@mobile/src/features/child/ChildMissionsScreen.tsx
@mobile/src/features/child/components/EmptyState.tsx
@mobile/src/features/child/components/FeedbackBanner.tsx
@mobile/src/api/types.ts
@mobile/src/navigation/routes.ts
@mobile/src/navigation/RootNavigator.tsx
@mobile/src/components/index.ts

<interfaces>
Plan 05-01 provides:
- childService.listPendingMissions(token, childId): Promise<AssignedMissionResponse[]>
- childService.completeMission(token, assignedMissionId): Promise<AssignedMissionResponse>
- childService.getWallet(token, childId): Promise<WalletResponse>
- ChildTabs route param includes the selected ChildResponse.

Mission response fields used here:
- status: PENDING, AWAITING_APPROVAL, COMPLETED, REJECTED, CANCELLED.
- snapshotTitle, snapshotDescription, snapshotCoinValue, snapshotRequiresApproval, dueDate.

Backend behavior:
- GET /children/{childId}/missions returns PENDING missions only.
- POST /assigned-missions/{id}/complete returns the resulting assigned mission response.
- If snapshotRequiresApproval is false, backend may return COMPLETED and credit the wallet.
- If snapshotRequiresApproval is true, backend returns AWAITING_APPROVAL and does not credit coins.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create mission tests and reusable mission components</name>
  <read_first>
    - mobile/src/features/child/__tests__/child-home.test.tsx
    - mobile/src/features/auth/__tests__/login-screen.test.tsx
    - mobile/src/components/__tests__/base-components.test.tsx
    - mobile/src/features/child/components/FeedbackBanner.tsx
    - mobile/src/features/child/components/EmptyState.tsx
    - mobile/src/components/CoinBadge.tsx
    - mobile/src/components/StatusBadge.tsx
    - .planning/phases/05-fluxo-da-crian-a/05-VALIDATION.md
    - .planning/phases/05-fluxo-da-crian-a/05-UI-SPEC.md
  </read_first>
  <files>mobile/src/features/child/__tests__/child-missions.test.tsx, mobile/src/features/child/components/MissionCard.tsx, mobile/src/features/child/components/ProgressBar.tsx</files>
  <behavior>
    - Test first: pending mission cards show title, description when present, `Pendente`, coin value, and `Concluir missão`.
    - Test first: mission card action has accessibility label `Concluir missão {title}`.
    - Test first: in-flight completion disables only the pressed mission button.
    - Test first: `AWAITING_APPROVAL` feedback renders `Missão enviada! Um responsável vai revisar.` and no coin-credit copy.
    - Test first: `COMPLETED` feedback renders `Missão concluída! +{coins} moedas` and a refreshed balance.
  </behavior>
  <action>Create `child-missions.test.tsx` before production changes using mocked `childService` and mocked `useAuth` session. Create `MissionCard` that composes `Card`, `CoinBadge`, `StatusBadge`, `PrimaryButton`, and optional detail action; map statuses to UI labels from 05-UI-SPEC: `Pendente`, `Aguardando aprovação`, `Concluída`, `Revisar com responsável`, and `Indisponível`. Create `ProgressBar` as a token-based 12px pill with clamped 0-100 value and an accessibility label/value, but only use it when a screen has real count data. Do not render color-only status, hidden buttons, raw enum names, or fake completed/total progress.</action>
  <verify>
    <automated>cd mobile && npm test -- child-missions --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - The new mission tests exist and fail before mission screen/component behavior is implemented.
    - MissionCard uses PT-BR visible labels and no backend enum names in visible copy.
    - ProgressBar clamps values and exposes accessible text; mission screens do not use it with invented totals.
  </acceptance_criteria>
  <done>Mission tests and reusable components define the expected list/detail/completion behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement mission list completion and backend refetch</name>
  <read_first>
    - mobile/src/features/child/ChildMissionsScreen.tsx
    - mobile/src/features/child/childService.ts
    - mobile/src/api/types.ts
    - mobile/src/features/child/components/MissionCard.tsx
    - mobile/src/features/child/components/FeedbackBanner.tsx
    - mobile/src/features/child/components/EmptyState.tsx
    - .planning/phases/05-fluxo-da-crian-a/05-CONTEXT.md
    - .planning/phases/05-fluxo-da-crian-a/05-RESEARCH.md
  </read_first>
  <files>mobile/src/features/child/ChildMissionsScreen.tsx, mobile/src/features/child/__tests__/child-missions.test.tsx</files>
  <behavior>
    - Test first: screen fetches wallet and pending missions with session.token and selected child id.
    - Test first: empty state renders `Nenhuma missão por aqui agora` and `Aproveite o descanso!`.
    - Test first: complete action calls childService.completeMission once when pressed repeatedly during loading.
    - Test first: after completion, screen refetches wallet and pending missions.
    - Test first: generic mutation errors render `Não conseguimos atualizar essa missão. Tente novamente.`
  </behavior>
  <action>Update `ChildMissionsScreen` to load `childService.getWallet` and `childService.listPendingMissions` for the selected child route param, render an `AppHeader` titled `Suas missões` with a `CoinBadge` for backend wallet balance, render subtitle `Você tem {count} missão pendente para hoje.` or `Hoje está tranquilo por aqui.`, and render pending missions through `MissionCard`. Implement `handleComplete(assignedMissionId)` with a per-mission in-flight id; duplicate presses on the same mission while loading must return without another service call. After `childService.completeMission`, preserve the returned `AssignedMissionResponse` in local feedback state, then refetch wallet and pending missions. If returned status is `AWAITING_APPROVAL`, show only `Missão enviada! Um responsável vai revisar.` and an `Aguardando aprovação` badge; if returned status is `COMPLETED`, show `Missão concluída! +{snapshotCoinValue} moedas` and the refreshed backend balance. Render `ApiError.userMessage` only when friendly; otherwise use the UI-SPEC error copy. Do not credit coins locally, do not keep completed missions in the pending list unless they are the immediate feedback response, and do not send familyUnitId.</action>
  <verify>
    <automated>cd mobile && npm test -- child-missions --runInBand</automated>
    <automated>cd mobile && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Completion refetch calls both wallet and pending missions after the mutation resolves.
    - Approval-required feedback contains no `+{coins}` text and no refreshed-balance promise beyond backend data.
    - Auto-credit feedback displays the completed response coin value and refreshed wallet balance from `getWallet`.
    - Only the mission being completed is disabled/loading; other mission cards remain stable.
  </acceptance_criteria>
  <done>Mission list supports safe completion with correct backend-tied feedback and refetch behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mission detail route and completion action</name>
  <read_first>
    - mobile/src/navigation/routes.ts
    - mobile/src/navigation/RootNavigator.tsx
    - mobile/src/features/child/ChildMissionsScreen.tsx
    - mobile/src/features/child/components/MissionCard.tsx
    - mobile/src/features/child/components/FeedbackBanner.tsx
    - mobile/src/components/AppHeader.tsx
    - mobile/src/components/SecondaryButton.tsx
    - .planning/phases/05-fluxo-da-crian-a/05-UI-SPEC.md
  </read_first>
  <files>mobile/src/navigation/routes.ts, mobile/src/navigation/RootNavigator.tsx, mobile/src/features/child/ChildMissionsScreen.tsx, mobile/src/features/child/ChildMissionDetailScreen.tsx, mobile/src/features/child/__tests__/child-missions.test.tsx</files>
  <behavior>
    - Test first: pressing `Ver detalhes` on a pending mission navigates to `ChildMissionDetail` with selected child and mission params.
    - Test first: detail screen shows title, description, `Recompensa`, `{coins} moedas`, due date when present, and approval helper when required.
    - Test first: detail `Marcar como concluída` calls completeMission, disables duplicate submits, and shows the same status-specific feedback as list completion.
    - Test first: error/not-found state renders `Não encontramos essa missão.` and `Voltar para missões`.
  </behavior>
  <action>Add `ChildMissionDetail: { child: ChildResponse; mission: AssignedMissionResponse }` to `RootStackParamList`, register it as an authenticated stack route in `RootNavigator`, and update `ChildMissionsScreen`/`MissionCard` so `Ver detalhes` navigates there. Create `ChildMissionDetailScreen` with an accessible `Voltar` secondary action, mission title/description, reward row `Recompensa` and `{snapshotCoinValue} moedas`, due date copy `Para {date}` when dueDate exists, approval helper `Um responsável vai revisar antes das moedas entrarem.` when `snapshotRequiresApproval` is true, and primary action `Marcar como concluída`. Reuse the same completion/refetch feedback semantics as the list: returned `AWAITING_APPROVAL` shows waiting copy only; returned `COMPLETED` shows earned coins and relies on backend refetch before balance display. Do not create a separate mission mutation path and do not expose raw backend status codes.</action>
  <verify>
    <automated>cd mobile && npm test -- child-missions --runInBand</automated>
    <automated>cd mobile && npm run typecheck</automated>
    <automated>cd mobile && npm test -- --runInBand</automated>
  </verify>
  <acceptance_criteria>
    - Root navigation compiles with `ChildMissionDetail` in the authenticated branch.
    - Detail completion has the same duplicate-submit protection and friendly error handling as list completion.
    - Detail screen does not show earned coins for approval-required missions.
    - Full mobile test suite remains green after route changes.
  </acceptance_criteria>
  <done>Child can inspect mission details and complete a mission from list or detail with consistent backend-sourced feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile mission screen -> mission endpoints | Selected child id and assignedMissionId cross from mobile route/UI state to protected backend endpoints. |
| mission mutation response -> UI feedback | Backend status determines coin or approval feedback shown to the child. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | ChildMissionsScreen/childService | mitigate | Use only token-authenticated childService calls; no familyUnitId field/path is accepted or sent. |
| T-05-02 | Elevation of Privilege / Information Disclosure | listPendingMissions/completeMission | mitigate | Backend family-scoped endpoints remain authority; mobile only passes selected child id and assignedMissionId from backend responses. |
| T-05-03 | Tampering | mission completion button | mitigate | Per-mission in-flight id blocks duplicate submits; backend INVALID_MISSION_STATUS remains final guard and is mapped to friendly copy. |
| T-05-05 | Information Disclosure | mission screens | mitigate | Do not display token, familyUnitId, accessPin, accessPinHash, raw enum names, HTTP details, or backend codes. |
| T-05-06 | Information Disclosure | mission error rendering | mitigate | Render UI-SPEC PT-BR messages or ApiError.userMessage only when user-safe; otherwise use `Não conseguimos atualizar essa missão. Tente novamente.` |
</threat_model>

<verification>
Run these commands after all tasks in this plan:
- `cd mobile && npm test -- child-missions --runInBand`
- `cd mobile && npm test -- child-service --runInBand`
- `cd mobile && npm run typecheck`
- `cd mobile && npm test -- --runInBand`
</verification>

<success_criteria>
- Pending mission list and mission detail both consume real backend pending missions.
- Completion returns clear backend-tied feedback for `COMPLETED` and `AWAITING_APPROVAL`.
- Duplicate mission submits are blocked in the UI while backend status validation remains authoritative.
- `child-missions.test.tsx` exists and passes.
</success_criteria>

<output>
Create `.planning/phases/05-fluxo-da-crian-a/05-02-SUMMARY.md` when done.
</output>
