---
phase: 05-fluxo-da-crian-a
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - mobile/src/navigation/routes.ts
  - mobile/src/navigation/RootNavigator.tsx
  - mobile/src/features/family/FamilyHubScreen.tsx
  - mobile/src/features/child/ChildProfileSelectScreen.tsx
  - mobile/src/features/child/ChildTabsScreen.tsx
  - mobile/src/features/child/ChildHomeScreen.tsx
  - mobile/src/features/child/components/BottomTabBar.tsx
  - mobile/src/features/child/__tests__/child-navigation.test.tsx
  - mobile/src/features/child/__tests__/child-home.test.tsx
autonomous: true
requirements:
  - MOBL-04
  - MOBL-05
  - MISS-04
  - WALT-01
user_setup: []

must_haves:
  truths:
    - "D-01: Child flow starts by selecting a real backend child profile from GET /children inside the responsible session."
    - "D-03/D-04: Switching profile/mode stays honest and does not fake responsible PIN validation."
    - "D-05/D-06/D-07/D-08: Child home shows identity, backend wallet balance, due-date-prioritized pending missions, and friendly loading/empty/error/retry states."
    - "D-17/D-19/D-20: Navigation and home UI use Expo React Native PT-BR screens, real API calls, and the approved UI-SPEC."
  artifacts:
    - path: "mobile/src/features/child/ChildProfileSelectScreen.tsx"
      provides: "Backend child profile selection"
    - path: "mobile/src/features/child/ChildTabsScreen.tsx"
      provides: "Child bottom-tabs container and profile tab content"
    - path: "mobile/src/features/child/ChildHomeScreen.tsx"
      provides: "Backend wallet balance and pending mission home summary"
    - path: "mobile/src/features/child/components/BottomTabBar.tsx"
      provides: "Custom token-based four-tab bar"
    - path: "mobile/src/features/child/__tests__/child-navigation.test.tsx"
      provides: "Profile selection and tab/profile navigation coverage"
    - path: "mobile/src/features/child/__tests__/child-home.test.tsx"
      provides: "Home balance, pending mission ordering, and state coverage"
  key_links:
    - from: "mobile/src/features/family/FamilyHubScreen.tsx"
      to: "ChildProfileSelect"
      via: "navigation.navigate('ChildProfileSelect')"
      pattern: "navigate\\('ChildProfileSelect'"
    - from: "mobile/src/features/child/ChildProfileSelectScreen.tsx"
      to: "childService.listChildren"
      via: "session.token from useAuth"
      pattern: "childService\\.listChildren"
    - from: "mobile/src/features/child/ChildHomeScreen.tsx"
      to: "childService.getWallet/listPendingMissions"
      via: "backend-sourced load/refetch"
      pattern: "childService\\.(getWallet|listPendingMissions)"
---

<objective>
Replace the child stub entry with real child profile selection, child tabs, home, and profile content.

Purpose: Let an authenticated family enter child mode with a real selected child and see backend balance/pending missions without fake PINs, fake profiles, or mock data.
Output: Child profile selection route, bottom tab shell, custom tab bar, home/profile tab content, and navigation/home tests.
</objective>

<execution_context>
@/home/dmagliano/.codex/get-shit-done/workflows/execute-plan.md
@/home/dmagliano/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fluxo-da-crian-a/05-CONTEXT.md
@.planning/phases/05-fluxo-da-crian-a/05-RESEARCH.md
@.planning/phases/05-fluxo-da-crian-a/05-VALIDATION.md
@.planning/phases/05-fluxo-da-crian-a/05-PATTERNS.md
@.planning/phases/05-fluxo-da-crian-a/05-UI-SPEC.md
@.planning/phases/05-fluxo-da-crian-a/05-01-SUMMARY.md
@mobile/AGENTS.md
@mobile/src/api/types.ts
@mobile/src/features/child/childService.ts
@mobile/src/features/auth/AuthContext.tsx
@mobile/src/navigation/routes.ts
@mobile/src/navigation/RootNavigator.tsx
@mobile/src/features/family/FamilyHubScreen.tsx
@mobile/src/components/index.ts
@mobile/src/theme/index.ts

<interfaces>
Plan 05-01 provides:
- ChildResponse, WalletResponse, AssignedMissionResponse DTOs.
- childService.listChildren(token), childService.getWallet(token, childId), childService.listPendingMissions(token, childId).
- @react-navigation/bottom-tabs installed for `createBottomTabNavigator`.

Existing auth context:
- useAuth() returns { status, session, errorMessage, login, logout, retryRestore }.
- session.token is the responsible JWT; selected child is route/UI state, not a new authenticated principal.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Child can demonstrate the core mission and reward loop on mobile using the real API | 05-01, 05-02, 05-03, 05-04 | COVERED | 05-02 provides the real entry and home surface.
REQ | MOBL-04 | Child profile selection/access and home with balance | 05-02 | COVERED | Profile selection, tabs, home, profile.
REQ | MOBL-05 | Mobile consumes the real backend API | 05-01, 05-02, 05-03, 05-04 | COVERED | 05-02 calls childService from 05-01.
REQ | MISS-04 | Child lists only own pending missions | 05-02, 05-03 | COVERED | Home summary consumes pending endpoint; mission screen completes list/detail.
REQ | MISS-05 | Child marks assigned mission completed | 05-03 | COVERED | Not in this plan's slice; covered by dependent plan.
REQ | REWD-03, REWD-04, REWD-05 | Reward catalog/redemption/insufficient balance | 05-04 | COVERED | Covered by final rewards plan.
REQ | WALT-01 | Child wallet exposes current balance | 05-02, 05-03, 05-04 | COVERED | Home/profile uses wallet balance; later screens reuse it.
RESEARCH | R-02 | Official bottom tabs and custom token-based tabBar | 05-01, 05-02 | COVERED | 05-02 implements the custom tabBar.
RESEARCH | R-03 | Pending missions endpoint returns pending only | 05-02, 05-03 | COVERED | Home renders pending count/list from endpoint.
RESEARCH | R-06 | No fake PIN and no mock/offline fallback | 05-02 | COVERED | Profile switch stays within existing session and errors remain visible.
RESEARCH | R-07 | Wave 0 navigation and home tests | 05-02 | COVERED | `child-navigation` and `child-home` tests.
CONTEXT | D-01, D-02, D-03, D-04 | Child entry and responsible-PIN boundary | 05-02 | COVERED | Real child selection; no fake responsible PIN.
CONTEXT | D-05, D-06, D-07, D-08 | Home hierarchy, sorting, and states | 05-02 | COVERED | Balance-first home and pending mission states.
CONTEXT | D-09..D-16 | Mission/reward feedback decisions | 05-03, 05-04 | COVERED | Covered by downstream mission/reward plans.
CONTEXT | D-17, D-18, D-19, D-20 | Stack, backend truth, real API, UI-SPEC | 05-02 | COVERED | Native RN UI with service calls and approved copy.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace child stub entry with backend profile selection</name>
  <read_first>
    - mobile/src/navigation/routes.ts
    - mobile/src/navigation/RootNavigator.tsx
    - mobile/src/features/family/FamilyHubScreen.tsx
    - mobile/src/features/auth/AuthContext.tsx
    - mobile/src/components/Card.tsx
    - mobile/src/components/EmojiAvatar.tsx
    - .planning/phases/05-fluxo-da-crian-a/05-UI-SPEC.md
    - .planning/phases/05-fluxo-da-crian-a/05-CONTEXT.md
  </read_first>
  <files>mobile/src/navigation/routes.ts, mobile/src/navigation/RootNavigator.tsx, mobile/src/features/family/FamilyHubScreen.tsx, mobile/src/features/child/ChildProfileSelectScreen.tsx, mobile/src/features/child/__tests__/child-navigation.test.tsx</files>
  <behavior>
    - Test first: pressing `Sou criança` in FamilyHub navigates to `ChildProfileSelect`.
    - Test first: ChildProfileSelect loads children from childService.listChildren(session.token).
    - Test first: `Entrar no perfil` is disabled until a backend child is selected.
    - Test first: selecting child `Joaquim` and pressing `Entrar no perfil` navigates to `ChildTabs` with the selected child object.
    - Test first: loading, empty, error, and retry states use UI-SPEC PT-BR copy.
  </behavior>
  <action>Update `RootStackParamList` so authenticated child routes are `ChildProfileSelect: undefined` and `ChildTabs: { child: ChildResponse }`; keep `Auth`, `FamilyHub`, and `ResponsibleStub`. Update `RootNavigator` to register `ChildProfileSelect` and `ChildTabs` in the authenticated branch and stop registering `ChildStub`. Update `FamilyHubScreen` so the `Sou criança` card navigates to `ChildProfileSelect` and no longer describes an unready child area. Create `ChildProfileSelectScreen` that calls `childService.listChildren(session.token)` from `useAuth`, renders only backend-returned active children, maps missing/unknown `avatarKey` to `⭐`, and shows the required copies `Quem vai brincar agora?`, `Escolha um perfil da família.`, `Nenhuma criança cadastrada`, `Peça para um responsável criar um perfil primeiro.`, `Não conseguimos carregar os perfis. Tente novamente.`, and `Entrar no perfil`. Per D-02/D-03/D-04, do not add a responsible PIN prompt, child PIN prompt, local PIN validation, or a child `User` session.</action>
  <verify>
    <automated>cd mobile && npm test -- child-navigation --runInBand</automated>
    <automated>cd mobile && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `FamilyHubScreen` no longer navigates to `ChildStub` for child entry.
    - `ChildProfileSelectScreen` has no hardcoded child names, balances, missions, or rewards.
    - Empty and error states include a `Tentar novamente` retry path when loading fails.
    - Tests prove selected child is navigation/UI state inside the existing responsible session.
  </acceptance_criteria>
  <done>Authenticated family users enter child mode by selecting a real backend child profile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build child tabs, home, and profile tab content</name>
  <read_first>
    - mobile/src/navigation/routes.ts
    - mobile/src/features/child/ChildProfileSelectScreen.tsx
    - mobile/src/components/AppScreen.tsx
    - mobile/src/components/AppHeader.tsx
    - mobile/src/components/Card.tsx
    - mobile/src/components/CoinBadge.tsx
    - mobile/src/components/PrimaryButton.tsx
    - mobile/src/components/SecondaryButton.tsx
    - mobile/src/theme/index.ts
    - .planning/phases/05-fluxo-da-crian-a/05-UI-SPEC.md
    - .planning/phases/05-fluxo-da-crian-a/05-VALIDATION.md
  </read_first>
  <files>mobile/src/features/child/ChildTabsScreen.tsx, mobile/src/features/child/ChildHomeScreen.tsx, mobile/src/features/child/components/BottomTabBar.tsx, mobile/src/features/child/__tests__/child-navigation.test.tsx, mobile/src/features/child/__tests__/child-home.test.tsx</files>
  <behavior>
    - Test first: child tabs expose `Abrir Início`, `Abrir Missões`, `Abrir Recompensas`, and `Abrir Perfil`.
    - Test first: ChildHome renders `Oi, Joaquim!`, `Seu tesouro`, backend wallet balance, and pending mission count.
    - Test first: home pending missions are ordered with due-dated missions before undated missions, then stable by dueDate/createdAt.
    - Test first: home loading, empty, error, and retry states render stable PT-BR copy.
    - Test first: profile tab content shows selected child identity, `Trocar criança`, `Voltar para família`, and `Sair da conta` without token/family id disclosure.
  </behavior>
  <action>Create `ChildTabsScreen` with `createBottomTabNavigator` from `@react-navigation/bottom-tabs`, using a custom `BottomTabBar` component and four destinations named `ChildHome`, `ChildMissions`, `ChildRewards`, and `ChildProfile`. The tab bar must use existing tokens/components, emojis only, minimum 44x44 tab targets, labels `Início`, `Missões`, `Recompensas`, and `Perfil`, and accessibility labels `Abrir Início`, `Abrir Missões`, `Abrir Recompensas`, and `Abrir Perfil`. In this plan, home and profile are the domain-complete tabs; mission and reward tab destinations must render honest non-domain content with no fake lists, balances, rewards, or completion actions because 05-03 owns mission screens and 05-04 owns reward screens. Implement `ChildHomeScreen` so it loads wallet and pending missions from `childService` with `session.token`; renders `Oi, {childName}!`, `Seu tesouro`, `{balance} moedas`, `Missões de hoje`, `Você tem {pendingCount} missão pendente hoje.`, and up to three backend pending missions. Implement `ChildProfile` tab content inside `ChildTabsScreen` with selected child identity, family name when available, `Trocar criança` navigating to `ChildProfileSelect`, `Voltar para família` navigating to `FamilyHub`, and `Sair da conta` calling logout. Per D-18/D-19, never subtract coins or invent reward/mission state locally; per D-20, do not copy Stitch HTML/CSS or import external visual assets.</action>
  <verify>
    <automated>cd mobile && npm test -- child-home --runInBand</automated>
    <automated>cd mobile && npm test -- child-navigation --runInBand</automated>
    <automated>cd mobile && npm run typecheck</automated>
    <automated>cd mobile && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `ChildTabsScreen` uses the official bottom-tabs package and a custom `BottomTabBar`.
    - Home balance uses `WalletResponse.balance` from `GET /children/{childId}/wallet`, not derived or cached subtraction.
    - Home pending mission summary uses `GET /children/{childId}/missions`; no completed/total progress is shown unless real count data exists.
    - Profile tab content does not display JWT, token, familyUnitId, accessPin, or accessPinHash.
    - `ChildMissionsScreen.tsx` and `ChildRewardsScreen.tsx` are not created by 05-02; those screen files are owned by 05-03 and 05-04.
  </acceptance_criteria>
  <done>Child profile selection reaches a real child tab shell, and home/profile tab content compiles and passes focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile navigation route params -> child UI | Selected ChildResponse crosses from profile selection into tab/home/profile screens. |
| mobile child UI -> Spring API | Responsible JWT and selected child id cross into protected wallet and mission endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | ChildProfileSelect/ChildTabs | mitigate | Selected child comes only from GET /children under responsible JWT; no child auth principal or client family id is created. |
| T-05-02 | Elevation of Privilege / Information Disclosure | ChildHome | mitigate | Wallet/mission reads use selected child id and backend family-scoped endpoints; no familyUnitId is accepted or sent. |
| T-05-05 | Information Disclosure | ChildProfile tab/AuthContext | mitigate | Keep responsible JWT inside AuthContext/SecureStore; do not render token, familyUnitId, accessPin, or accessPinHash. |
| T-05-06 | Information Disclosure | Profile/home error rendering | mitigate | Render friendly UI-SPEC copy or ApiError.userMessage only when user-safe; never render stack traces, HTTP details, or enum names. |
</threat_model>

<verification>
Run these commands after all tasks in this plan:
- `cd mobile && npm test -- child-navigation --runInBand`
- `cd mobile && npm test -- child-home --runInBand`
- `cd mobile && npm run typecheck`
- `cd mobile && npm run lint`
</verification>

<success_criteria>
- Plan touches 9 files, below the scope_sanity warning threshold.
- `ChildStub` is no longer the child entry route.
- Profile selection, tabs shell, home, and profile tab content consume real backend data where data is displayed.
- Navigation and home tests exist and pass.
- No fake/offline child, wallet, mission, reward, PIN, token, or family authorization data is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-fluxo-da-crian-a/05-02-SUMMARY.md` when done.
</output>
