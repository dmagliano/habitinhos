---
phase: 05-fluxo-da-crian-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - mobile/package-lock.json
  - mobile/src/api/types.ts
  - mobile/src/features/child/childService.ts
  - mobile/src/features/child/__tests__/child-service.test.ts
autonomous: true
requirements:
  - MOBL-04
  - MOBL-05
  - MISS-04
  - MISS-05
  - REWD-03
  - REWD-04
  - REWD-05
  - WALT-01
user_setup: []

must_haves:
  truths:
    - "D-17/D-18/D-19: Mobile child data uses Expo React Native TypeScript and real backend API calls only."
    - "D-01/D-18: Selected child ids are backend child profile ids, not a child-authenticated User session."
    - "D-19/R-01: Reward redemption uses POST /rewards/{id}/redeem with body { childId }, with no mock/offline fallback."
  artifacts:
    - path: "mobile/src/api/types.ts"
      provides: "Child, wallet, assigned mission, reward, and reward redemption DTO contracts"
    - path: "mobile/src/features/child/childService.ts"
      provides: "Typed child flow service methods through apiRequest"
      exports: ["childService"]
    - path: "mobile/src/features/child/__tests__/child-service.test.ts"
      provides: "Service path, token, body, and no-familyUnitId coverage"
  key_links:
    - from: "mobile/src/features/child/childService.ts"
      to: "mobile/src/api/client.ts"
      via: "apiRequest<T>(path, { token, method, body })"
      pattern: "apiRequest<.*>\\("
    - from: "mobile/src/features/child/childService.ts"
      to: "POST /rewards/{id}/redeem"
      via: "redeemReward(token, rewardId, childId)"
      pattern: "rewards/.*/redeem"
---

<objective>
Create the Phase 5 child API foundation: bottom-tabs dependency, mobile DTO contracts, child service layer, and service tests.

Purpose: Give later child screens one tested backend access layer while preserving D-17, D-18, D-19, current route research, and the existing `apiRequest` family isolation guard.
Output: Expo-compatible bottom-tabs dependency, typed child API DTOs, `childService`, and `child-service` tests.
</objective>

<execution_context>
@/home/dmagliano/.codex/get-shit-done/workflows/execute-plan.md
@/home/dmagliano/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fluxo-da-crian-a/05-CONTEXT.md
@.planning/phases/05-fluxo-da-crian-a/05-RESEARCH.md
@.planning/phases/05-fluxo-da-crian-a/05-VALIDATION.md
@.planning/phases/05-fluxo-da-crian-a/05-PATTERNS.md
@.planning/phases/05-fluxo-da-crian-a/05-UI-SPEC.md
@mobile/AGENTS.md
@mobile/package.json
@mobile/src/api/client.ts
@mobile/src/api/types.ts
@mobile/src/features/auth/authService.ts
@mobile/src/features/auth/__tests__/api-auth.test.ts
@docs/api-contract.md

<interfaces>
Existing mobile API wrapper:
- apiRequest<T>(path: string, options?: { method?: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE'; body?: unknown; token?: string | null; headers?: Record<string, string> }): Promise<T>
- ApiError exposes status, code, details, userMessage, isNetworkError, and isSessionExpired.
- apiRequest rejects any client body/path containing familyUnitId.

DTO contracts to add in mobile/src/api/types.ts:
- ChildResponse: id, name, age, avatarKey, active, createdAt, updatedAt.
- WalletResponse: childId, balance, createdAt, updatedAt.
- AssignedMissionStatus: PENDING, AWAITING_APPROVAL, COMPLETED, REJECTED, CANCELLED.
- AssignedMissionResponse: id, missionId, childId, status, dueDate, completedAt, approvedAt, rejectedAt, rejectionReason, snapshotTitle, snapshotDescription, snapshotCoinValue, snapshotRequiresApproval, createdAt, updatedAt.
- RewardResponse: id, title, description, cost, active, createdAt, updatedAt.
- RewardRedemptionStatus: REDEEMED.
- RewardRedemptionResponse: id, rewardId, childId, walletId, status, snapshotTitle, snapshotCost, coinTransactionId, createdAt, updatedAt.

Backend route reality:
- GET /children
- GET /children/{childId}/wallet
- GET /children/{childId}/missions
- POST /assigned-missions/{id}/complete
- GET /rewards
- POST /rewards/{id}/redeem with body { childId }
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Child can demonstrate the core mission and reward loop on mobile using the real API | 05-01, 05-02, 05-03, 05-04 | COVERED | 05-01 creates API foundation; 05-02 entry/home; 05-03 missions; 05-04 rewards and final validation.
REQ | MOBL-04 | Profile selection/access, home, missions, feedback, rewards, redemption, insufficient balance | 05-02, 05-03, 05-04 | COVERED | UI slices are split by child workflow area.
REQ | MOBL-05 | Mobile consumes the real backend API for demo flow | 05-01, 05-02, 05-03, 05-04 | COVERED | childService uses apiRequest; screens call childService.
REQ | MISS-04 | Child lists only own pending missions | 05-01, 05-02, 05-03 | COVERED | Service path plus home/mission screen consumption.
REQ | MISS-05 | Child marks own assigned mission completed | 05-01, 05-03 | COVERED | Service completeMission plus mission list/detail.
REQ | REWD-03 | Child lists active family rewards | 05-01, 05-04 | COVERED | Service listRewards plus rewards screen.
REQ | REWD-04 | Child redeems reward with sufficient balance | 05-01, 05-04 | COVERED | Current backend redeem route plus confirmation flow.
REQ | REWD-05 | System blocks insufficient balance | 05-04 | COVERED | Disabled affordance and backend INSUFFICIENT_BALANCE handling.
REQ | WALT-01 | Child wallet exposes current balance | 05-01, 05-02, 05-03, 05-04 | COVERED | Service getWallet consumed by home, missions, rewards.
RESEARCH | R-01 | Use POST /rewards/{id}/redeem with { childId } | 05-01, 05-04 | COVERED | Route drift resolved to backend reality.
RESEARCH | R-02 | Install official @react-navigation/bottom-tabs and custom token-based tabBar | 05-01, 05-02 | COVERED | Package install in 05-01; custom tab UI in 05-02.
RESEARCH | R-03 | Pending missions endpoint returns pending only; preserve mutation response and refetch wallet/list | 05-01, 05-03 | COVERED | Mission implementation owns refetch behavior.
RESEARCH | R-04 | Approval-required vs auto-credit feedback | 05-03 | COVERED | Status-specific mission feedback.
RESEARCH | R-05 | Reward affordability is UI-only; backend enforces INSUFFICIENT_BALANCE | 05-04 | COVERED | Confirmation plus backend error mapping.
RESEARCH | R-06 | No fake local PIN validation and no mock/offline fallback | 05-02, 05-03, 05-04 | COVERED | Plans explicitly prohibit fake PIN and mock data.
RESEARCH | R-07 | Wave 0 tests for service, navigation, home, missions, rewards | 05-01, 05-02, 05-03, 05-04 | COVERED | Each plan creates its focused tests.
CONTEXT | D-01, D-02, D-03, D-04 | Child entry and responsible-PIN boundary | 05-02 | COVERED | Select real children and keep switch honest without fake PIN.
CONTEXT | D-05, D-06, D-07, D-08 | Home hierarchy, ordering, and states | 05-02 | COVERED | Balance-first home with pending missions and retry states.
CONTEXT | D-09, D-10, D-11, D-12 | Mission completion feedback | 05-03 | COVERED | Backend-tied banners/cards, no animation requirement.
CONTEXT | D-13, D-14, D-15, D-16 | Reward confirmation and balance states | 05-04 | COVERED | Confirmation, disabled insufficient state, success/refetch.
CONTEXT | D-17, D-18, D-19, D-20 | Stack, source of truth, real API, UI-SPEC | 05-01, 05-02, 05-03, 05-04 | COVERED | All plans use Expo RN TS, backend truth, real API, approved UI-SPEC.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add bottom-tab dependency and child DTO contracts</name>
  <read_first>
    - mobile/package.json
    - mobile/src/api/types.ts
    - .planning/phases/05-fluxo-da-crian-a/05-RESEARCH.md
    - .planning/phases/05-fluxo-da-crian-a/05-PATTERNS.md
    - docs/api-contract.md
  </read_first>
  <files>mobile/package.json, mobile/package-lock.json, mobile/src/api/types.ts</files>
  <action>Run `cd mobile && npx expo install @react-navigation/bottom-tabs` exactly, using the approved Package Legitimacy Audit in 05-RESEARCH. Add the DTO contracts listed in this plan's interfaces to `mobile/src/api/types.ts`, using `string` for UUID/Instant/LocalDate and `number` for Java int. Keep existing `ApiError` and auth DTO exports intact. This supports MOBL-05 plus the API contracts used by MOBL-04, MISS-04, MISS-05, REWD-03, REWD-04, REWD-05, and WALT-01; do not add visual packages, React Query, mock data, offline fallback, or child authentication per D-17/D-18/D-19/D-20.</action>
  <verify>
    <automated>cd mobile && npm run typecheck</automated>
    <automated>cd mobile && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `@react-navigation/bottom-tabs` is present in `mobile/package.json` and `mobile/package-lock.json`.
    - DTO names and fields match current backend records from 05-RESEARCH.
    - Existing auth/API tests still compile against `mobile/src/api/types.ts`.
  </acceptance_criteria>
  <done>Bottom-tabs dependency and Phase 5 DTO contracts are available to later child screen plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement childService with real backend route tests</name>
  <read_first>
    - mobile/src/api/client.ts
    - mobile/src/api/types.ts
    - mobile/src/features/auth/authService.ts
    - mobile/src/features/auth/__tests__/api-auth.test.ts
    - .planning/phases/05-fluxo-da-crian-a/05-RESEARCH.md
  </read_first>
  <files>mobile/src/features/child/childService.ts, mobile/src/features/child/__tests__/child-service.test.ts</files>
  <behavior>
    - Test first: listChildren(token) calls GET /children with Bearer token and no familyUnitId.
    - Test first: getWallet(token, childId) calls GET /children/{childId}/wallet.
    - Test first: listPendingMissions(token, childId) calls GET /children/{childId}/missions.
    - Test first: completeMission(token, assignedMissionId) calls POST /assigned-missions/{id}/complete.
    - Test first: listRewards(token) calls GET /rewards.
    - Test first: redeemReward(token, rewardId, childId) calls POST /rewards/{id}/redeem with body { childId }.
  </behavior>
  <action>Create `mobile/src/features/child/childService.ts` exporting `childService` with `listChildren`, `getWallet`, `listPendingMissions`, `completeMission`, `listRewards`, and `redeemReward`. Every method must call `apiRequest` with the responsible `token`, must not accept or send `familyUnitId`, and must use the current backend reward route per D-18/D-19 and R-01. Write `child-service.test.ts` before implementation using the existing fetch/mock style from `api-auth.test.ts`; include an explicit assertion that redemption posts to `/rewards/reward-1/redeem` with `{"childId":"child-1"}`. Do not add any mock/offline fallback path when fetch fails.</action>
  <verify>
    <automated>cd mobile && npm test -- child-service --runInBand</automated>
    <automated>cd mobile && npm run typecheck</automated>
    <automated>cd mobile && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `child-service.test.ts` fails before `childService` exists and passes after implementation.
    - Fetch calls include `Authorization: Bearer jwt-token` when a token is supplied.
    - No child service method accepts a family id or includes `familyUnitId` in path/body.
    - Redemption uses `POST /rewards/{id}/redeem` with body `{ childId }`, preserving the resolved 05-RESEARCH Open Questions.
  </acceptance_criteria>
  <done>childService covers all Phase 5 backend paths and targeted service tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile child service -> Spring API | Responsible JWT and selected child/reward/mission ids cross into protected backend endpoints. |
| package install -> mobile runtime | New npm dependency enters the Expo mobile runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | childService/apiRequest | mitigate | `childService` never accepts familyUnitId; existing `apiRequest` guard rejects familyUnitId in path/body; child-service tests assert route payloads. |
| T-05-02 | Tampering | reward redemption route | mitigate | Service test locks the current backend route `POST /rewards/{id}/redeem` with body `{ childId }`, avoiding stale docs drift. |
| T-05-06 | Information Disclosure | ApiError propagation | mitigate | Keep raw backend codes/details inside ApiError; service does not render or transform them into child-visible copy. |
| T-05-SC | Tampering | npm install for @react-navigation/bottom-tabs | mitigate | Use `cd mobile && npx expo install @react-navigation/bottom-tabs`; Package Legitimacy Audit marks it Approved with slopcheck OK and no [ASSUMED]/[SUS] package. |
</threat_model>

<verification>
Run these commands after all tasks in this plan:
- `cd mobile && npm test -- child-service --runInBand`
- `cd mobile && npm run typecheck`
- `cd mobile && npm run lint`
</verification>

<success_criteria>
- Plan touches 5 files, below the scope_sanity warning threshold.
- DTOs and `childService` expose all Phase 5 child/wallet/mission/reward backend contracts.
- Service tests prove token use, route correctness, no `familyUnitId`, and current reward redemption route.
- No fake PIN, child auth, mock data, or offline fallback is introduced.
</success_criteria>

<output>
Create `.planning/phases/05-fluxo-da-crian-a/05-01-SUMMARY.md` when done.
</output>
