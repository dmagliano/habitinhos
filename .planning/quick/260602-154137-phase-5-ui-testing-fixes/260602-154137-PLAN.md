---
mode: quick-full
quick_id: 260602-154137
slug: phase-5-ui-testing-fixes
status: planned
created: 2026-06-02
must_haves:
  truths:
    - Login fields remain visible when the Android keyboard opens.
    - Child bottom tab labels remain on one line and sit above Android navigation controls.
    - Child profile avatar emoji is not clipped vertically.
  artifacts:
    - mobile/src/features/auth/LoginScreen.tsx
    - mobile/src/features/child/components/BottomTabBar.tsx
    - mobile/src/components/EmojiAvatar.tsx
    - targeted React Native tests
  key_links:
    - docs/design/review/Screenshot_20260602_132200_Expo Go.jpg
    - docs/design/review/Screenshot_20260602_132257_Expo Go.jpg
---

# Quick Task 260602-154137: Phase 5 UI Testing Fixes

Apply manual Phase 5 UI smoke findings without waiting for the full child smoke after Phase 6.

## Task 1: Make login keyboard-safe on Android

files:
- `mobile/src/features/auth/LoginScreen.tsx`
- `mobile/src/features/auth/__tests__/login-screen.test.tsx`

action:
- Use keyboard avoidance on Android as well as iOS.
- Keep the login form scrollable enough that focused fields can move above the keyboard.

verify:
- Login test covers the Android keyboard avoiding behavior.
- Existing login render and submit tests still pass.

done:
- Keyboard no longer overlays the login fields in the manual screenshot scenario.

## Task 2: Make child bottom tabs safe-area aware and single-line

files:
- `mobile/src/features/child/components/BottomTabBar.tsx`
- `mobile/src/features/child/__tests__/child-navigation.test.tsx`

action:
- Read bottom safe-area insets and include them in tab-bar bottom padding.
- Constrain tab labels to one line so `Recompensas` does not wrap.
- Slightly tighten tab label typography only inside the navigation bar where needed.

verify:
- Navigation test asserts all four tabs render, including the long rewards tab.
- Source uses safe-area insets and one-line tab labels.

done:
- Bottom tabs remain visible above Android navigation controls and labels stay intact.

## Task 3: Prevent emoji avatar clipping

files:
- `mobile/src/components/EmojiAvatar.tsx`
- `mobile/src/components/__tests__/base-components.test.tsx`

action:
- Give avatar emoji text an explicit centered line height based on avatar size.
- Keep existing avatar dimensions and labels intact.

verify:
- Base component test covers an xl star avatar.
- Existing component tests still pass.

done:
- The child profile star emoji is visible without top/bottom clipping.

## Inline Plan Check

Result: passed.

- Requirement coverage: all three manual findings are mapped to tasks and files.
- Task completeness: each task includes files, action, verify, and done fields.
- Key links: referenced source files and review screenshots exist.
- Scope sanity: three focused UI tasks are appropriate for a quick task.
- Must-haves: trace directly to the user-reported expected behavior.
